# src/

Your project lives here — any language, any structure that fits the work. Replace this file
with a real map of the code once there is one.

The only rule: the project is in `src/`, the process is in `agent-sessions/` and `reports/`.
Keeping them separate is what lets practitioners review your *work* and your *way of working*
independently.
