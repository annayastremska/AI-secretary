# secrets & local env — never in the repo
.env
.env.*
*.pem
*.key

# junk
.DS_Store
__pycache__/
*.pyc
node_modules/
.venv/
venv/
dist/
build/

# NOTE: agent-sessions/**/*.jsonl is intentionally NOT ignored — exported session
# logs are part of the course contract. Scrub secrets before committing them.

# ─────────────────────────────────────────────────────────────
# ГОЛОВНЕ: реальні документи замовника в git не потрапляють.
# Це правило проєкту, не зручність. Не послаблювати без Дениса.

# Реальні документи — тільки локально, ніколи в git
data-private/
data-real/
*.real
*.real.*

# Скани й фото документів. Синтетичні зразки — виняток нижче
*.pdf
*.jpg
*.jpeg
*.png
*.tif
*.tiff
*.heic
*.docx
*.doc
*.xlsx
*.xls

# Виняток: синтетика і схеми, які свідомо тримаємо в репо.
# ЄДИНЕ місце для файлів документів -- data/eval/ (зразки + еталон). Реальні
# документи не пускає ні цей виняток, ні що інше: вони живуть у data-private/
# і в data/inbox/, обидві заблоковані вище й нижче.
!data/eval/**
!docs/**/*.png
!docs/**/*.svg

# Виняток другий: порожні бланки генератора. Це публічні форми (Додаток 28 і
# Додаток 30 до Інструкції з діловодства у ЗСУ) без жодного заповненого поля --
# з них генератор робить синтетичні документи. Реальних документів тут не буває.
!data/generator/templates/*.docx

# Дампи бази — можуть містити витягнуті реальні дані.
# Схема (DDL) у git потрібна, тому *.sql цілком не блокуємо
dumps/
*.dump
backup*.sql
*_dump.sql
*.sqlite
*.sqlite3
*.db

# Секрети
!.env.example
credentials.json
service-account*.json

# Python
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# Моделі й ваги — важке, в git не тягнемо
*.gguf
*.safetensors
*.bin
models/

# Службове
.idea/
.vscode/
*.log

# ─────────────────────────────────────────────────────────────
# pipeline (Аня): локальний стан прогонів, не частина коду

# Папка-приймач: робочі файли конкретної машини. Сам каталог лишається
# (через .gitkeep), щоб `python run_pipeline.py` працював на свіжому клоні.
data/inbox/*
!data/inbox/.gitkeep

# Результат прогонів (документи + індекс хешів) -- генерується заново.
# ДВІ папки навмисно, за призначенням, а не за зручністю:
#   data/output/      -- наші ТЕСТОВІ прогони. Сміття, яке можна видаляти
#                        будь-коли; сюди ж падає старий корпус LEAVE/TRIP,
#                        чиї люди не сходяться зі штаткою.
#   data/output-demo/ -- ТІЛЬКИ демо-набір. У базу віддається вміст рівно цієї
#                        папки, тому вона мусить бути чистою від тестів.
data/output/
data/output-demo/

# Архів оброблених вхідних файлів -- локальний стан конкретної машини.
data/processed/
data/failed/

# Локальний конфіг: налаштування конкретної машини (шляхи, наявність
# моделі). У репо лежить лише config.example.yaml -- скопіюйте його.
config.yaml

# Звіти оцінювача -- генеровані виходи прогонів. Самі цифри й що вони
# означають фіксуються в документації; JSON лишається на диску для діфу
# між прогонами, але в історію коду не йде. Правило стоїть ПІСЛЯ !data/eval/**
# -- останній збіг перемагає, тому виняток вище звіти не повертає.
data/eval/reports/

# Worktree-и агентів: копії репозиторію всередині репозиторію (21 МБ),
# створюються харнесом на час роботи агента.
.claude/

# Кеш ваг і моделей поруч із кодом.
.cache/

# Архіви цілим класом: саме архівом найпростіше занести пачку реальних
# сканів одним `git add`, обійшовши перелік розширень вище.
*.zip
*.rar
*.7z
*.tar
*.tar.gz

# Файли-замки Word: відкритий .docx створює поруч ~$-копію.
~$*

# db (Андрій): важкий ML-стек окремим venv (torch/transformers конфліктували
# б з легким .venv для БД), і локальні дані контейнерів (bind-mount, не
# частина коду -- генеруються docker compose up).
.venv-ml/
airflow/logs/
data/postgres/
data/originals/
data/.gradio-cache/

# QR гостьового входу: у картинці ключ доступу, тому вона НЕ в git.
# Складається локально при розгортанні й копіюється на сервер.
data/qr-guest.png

# Машинний слід ходів чата: у ньому питання користувачів.
logs/chat-trace.jsonl
# Слід режиму створення документа. Значень полів у ньому НЕМА за побудовою
# (перевірено 29.08: ні ПІБ, ні місць, ні дат) -- лишаються код звернення, вид
# бланка й назва події. Але це однаково рантайм-вивід, а не код, і в git йому
# місця немає: у діффі він давав би шум на кожен прогін.
logs/docgen-trace.jsonl

# Зібрана канва: 2 МБ редактора в кожному файлі, у git їй місця немає.
docs/deck/canvas/ai-secretary-demo-day.html

# QR і пам'ятка журі: у кожному зашитий ключ гостьового доступу.
# Складаються локально (docs/demo/make-jury-qr.py) і копіюються
# на сервер -- як і data/qr-guest.png.
data/qr-jury.png
data/jury-guide.html

# Завантажений нормативний корпус -- похідне, 16 МБ. Відтворюється
# db/scripts/load_normative_corpus.py за переліком у
# docs/tasks/2026-08-21_normative-corpus.md. Те саме правило, що для дампів.
data/eval/samples/normative/downloaded/

# Службові файли MinIO. Сховище прибране 20.08.2026, а його внутрішній стан
# (.minio.sys/*.meta) ніколи не мав бути в git: це не наші дані, а кеш
# сервера, який змінюється кожного запуску контейнера.
data/minio/
