# Опційні залежності. Кожен блок незалежний -- ставте лише те, що реально
# потрібно для вашого сценарію. Разом вони важать кілька ГБ, і НІ ОДНА з них
# не потрібна, щоб прогнати пайплайн на .docx.

# --- LLM-шар (поля extraction: llm, тайбрейк при визначенні шаблону) ---
# Ваги моделі в репозиторії НЕ лежать (.gguf у .gitignore) -- завантажте
# окремо з huggingface.co і вкажіть шлях у config.yaml -> llm.model_path:
#   INSAIT-Institute/MamayLM-Gemma-3-12B-IT-v2.0-GGUF  (~6.8 ГБ, Q4_K_M)
#   INSAIT-Institute/MamayLM-Gemma-3-4B-IT-v1.0-GGUF   (~2.5 ГБ, для слабших машин)
# На CPU-ноуті заміряно: ~257 с одноразове завантаження ваг 12B + ~56 с на
# документ з однією групою полів.
llama-cpp-python>=0.3.0

# --- OCR для фото/сканів (не потрібен для .docx) ---
# Тягне torch і Pillow; при першому запуску вантажить власні ваги (~1.5 ГБ).
# Всередині запускає llama.cpp-сервер: на Windows зручніше вказати готовий
# бінарник у config.yaml -> ocr.llama_server_path, ніж збирати з джерел.
surya-ocr

# --- Сховище й БД замість локальних файлів ---
# УВАГА: обидві реалізації (pipeline/storage/minio_store.py,
# postgres_writer.py) НЕ перевірені на живих сервісах -- ставтесь як до
# чернетки до першого реального прогону.
minio
psycopg[binary]
