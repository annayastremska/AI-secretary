# Андрію: видалити 3 документи, які я залила під час перевірки

_26.08.2026, Аня. Коротко: моє сміття в базі, треба прибрати. Видалення — запис
у базу, тобто твоя зона, тому прошу тебе, а не роблю сама._

## Що сталося

Я перевіряла, як апка поводиться з поламаними файлами (порожній `.docx` і
файл, який лише називається `.docx`). Апка повелася правильно: позначила
«не розпізнано» і попередила, що в базу піде лише сам документ. Але я на них
ще й натиснула «підтвердити» — тобто сама виконала крок, який пише в базу.
Треба було зупинитись на показі полів.

## Що прошу видалити

| id | стан | домен | фактів | що це |
|---|---|---|---|---|
| 248 | extracted | — | 0 | `довідка_лікування` з holdout — тестове завантаження |
| 249 | failed | — | 0 | `b1-broken.docx` — файл, який не є docx |
| 250 | failed | — | 0 | `b1-empty.docx` — порожній файл (0 байт) |

Разом із ними — три завдання в черзі: `review_queue.id` **167, 168, 169**
(усі `unknown_type`, `resolved_at IS NULL`, кожне вказує на свій документ).

**Перевірено перед проханням:** фактів у цих документів **нуль**, у
`fact_sources` посилань на них **нуль**. Тобто нічого, крім самих трьох рядків
і трьох завдань, не зачеплено.

## Чого НЕ треба чіпати

* **245** — це справжній відпускний квиток (`LEAVE-001.pdf`, 9 фактів), я
  залила його свідомо як живу перевірку наскрізного шляху. Він є і у виході
  обробки, і в базі — тут усе сходиться, лишаємо.
* **251, 252, 253** — це твої три внутрішні інструкції, залиті сьогодні.

## Як видалити (порядок важливий)

```sql
BEGIN;
-- 1) завдання черги
DELETE FROM review_queue WHERE id IN (167, 168, 169);
-- 2) самі документи (фактів і джерел у них немає -- перевірено)
DELETE FROM documents WHERE id IN (248, 249, 250);
-- 3) контроль ПЕРЕД фіксацією: мусить бути 201
SELECT count(*) AS documents FROM documents;
COMMIT;
```

Якщо третій рядок дасть не 201 — не фіксуй, скажи мені.

## Чому це важливо (одне речення)

Прилад звірки каталогу порівнює вихід обробки з базою; поки ці три рядки там,
він показує 4 розбіжності — тобто наш головний доказ правильності цифр не
зелений, і на сторінці статистики стоїть 204 документи замість 201 та 5
«не розпізнано» замість 2.

## Як перевірити, що стало добре

```bash
cd ~/anya/ai-secretary && set -a && . ./.env && set +a
.venv/bin/python demos/upload_app/verify_catalog.py --output-dir data/output-demo | tail -3
```
Очікується: `усі шаблони зійшлись із незалежним підрахунком.`

---

**Друге прохання, окремим листом:** `2026-08-26_for-andriy-roster-matching.md`
— статус фактів по особі, якої немає у штатці, і закриття старих завдань
«нова особа» (133 із 24.08).
