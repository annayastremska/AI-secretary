# extraction

За обраною схемою (`schemas/`) — regex для формальних полів, LLM (MamayLM)
для вільнотекстових. Джерело полів і типу екстракції — `fields[].extraction`
у відповідному файлі `schemas/*.yaml`.
