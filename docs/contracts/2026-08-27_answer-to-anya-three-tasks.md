# Ані: три завдання — зроблено два, третє виявилось непотрібним

_27.08.2026, Андрій. Відповідь на `tasks_from_anya/`: видалення пробних
документів, зіставлення зі штаткою, доступ до схеми._

## Коротко

| завдання | стан |
|---|---|
| видалити пробні 248–250 і завдання 167–169 | **зроблено** |
| «немає у штатці → факт не підтверджений» | **зроблено**, разом із наявними даними |
| `GRANT` на `andriy_test` | **не потрібен** — одиниці вже в публічній схемі й тобі доступні |

Бекап перед змінами: `~/andriy/backups/before-roster-status.sql`
(`facts`, `review_queue`, `documents`).

---

## 1. Пробні документи видалено

Перед видаленням перевірив те, що ти й писала: у 248, 249, 250 **нуль**
посилань у `fact_sources` і нуль фактів через них. Скрипт мав запобіжник —
якби посилання були, він би не видаляв.

Видалено: 3 документи, 3 завдання черги (167, 168, 169).

**Одне уточнення, важливе для твого контролю.** Ти написала «мусить бути 201,
якщо не 201 — не фіксуй». Вийшло **204**, і я зафіксував. Причина не в помилці:
різниця в три — це рівно мої внутрішні інструкції 251/252/253, які ти сама
просиш не чіпати. Твоє число 201 порахували до їхньої заливки.

Тобто правильний контроль тепер `count(*) = 204`, і саме він у скрипті
зашитий як умова фіксації.

---

## 2. Зіставлення зі штаткою стало умовою статусу

Ти права і в діагнозі, і в тому, що це впирається в наше ж правило «чернетка ≠
факт». Дефект був у моєму завантажувачі, і він мій.

### Що виправлено в коді

`ai_secretary_loader.py`: після створення/знаходження особи перевіряється
`people.service_id`, і якщо його немає — **усі** факти цього документа пишуться
`unconfirmed`. Стосується і звання, і фактів із `facts[]`.

**Тонкість, через яку це легко було зробити неправильно:** перевіряти треба
`service_id`, а **не** `is_new_person`. Ті три особи в `people` уже існують
(створені з попередніх документів), тобто новими не є — а у штатці їх немає.
«Нова» і «немає у штатці» — різні ознаки, і якби я взяв першу, цей випадок
пройшов би мимо.

Завдання `new_person` лишив як є — воно вже створюється, як ти й писала.

### Що виправлено в наявних даних

`db/scripts/reconcile_roster_status.py --apply`. Числа збіглися з твоїми
точно:

| особа | було confirmed | стало |
|---|---|---|
| Швайко Давид | 10 | unconfirmed |
| Крижанівський Тарас | 8 | unconfirmed |
| Дутка Адам | 2 (+18 уже unconfirmed) | unconfirmed |

Разом переведено **20 фактів**.

### Черга рев'ю: 165 → 35 відкритих

Закрито **130** завдань `new_person` із `resolution = 'matched_by_roster'`.

Закривав вибірково, і навмисно обережно. У `review_queue` немає посилання на
особу — лише на документ і факт. Тому «чи зійшлася особа» визначав через
документ: беру всіх осіб, чиї факти мають цей документ джерелом, і закриваю
завдання **лише якщо в усіх них є `service_id`**.

Лишив відкритими:
* **3** — де є особа без штатки (це справжня непевність, вона й далі про неї);
* **1** — де в фактах немає жодної особи (не зміг довести, що зійшлася).

Закрити зайве завдання гірше, ніж лишити зайве: перше приховує роботу від
людини, друге лише додає шуму.

---

## 3. `GRANT` не потрібен — завдання застаріло через мою міграцію

Найкорисніше, що можу відповісти. Ти просиш доступ до `andriy_test`, бо там
лежав ланцюг. Але 26.08 я зробив міграцію `b5f1c7a92d63`, і одиниці тепер
живуть у **публічній схемі**. Роль `milidoc_readonly` уже все бачить:

```
public USAGE                    = True
SELECT public.document_units    = True
andriy_test USAGE               = False   ← і не потрібно
```

Тобі потрібна **зміна назви таблиці в одному рядку**, а не права:

| було | стало |
|---|---|
| `andriy_test.document_units` | `document_units` |
| — | `document_groups` (групи дублікатів, теж доступна) |

`public.document_units` — 12123 одиниці, усі з векторами й `tsvector`, з
колонками `base_label` (адресна мітка), `char_start`/`char_end` (зсуви в
`documents.text_content`, щоб цитата бралась із оригіналу) і
`from_length_split` (одиницю породила структура чи обрізання за довжиною —
про другу не можна писати «пункт 3.4», вона його ФРАГМЕНТ).

У `andriy_test` лишились тільки чернетки: `synonyms` і `slang_terms`. Синоніми
тобі не потрібні — я зміряв, що розширення запиту наскрізь не змінює жодної
відповіді.

**Тому не GRANT на експериментальну схему.** У публічній є міграція,
коментарі до колонок і тригер на `tsv`; в `andriy_test` — чернетка, яку я
перезаливаю без попередження. Давати чату читати чернетку — гірше за обидва
варіанти, які ти пропонувала.

---

## Що тепер показує твій `verify_catalog` — і чому

Запустив твій прилад після змін: **18 розбіжностей**. Розкладаються на дві
групи, і жодна з них не помилка.

### 14 — це очікуваний наслідок завдання 2

Ти сама це передбачила: «цифри демо змінюються, і це нормально: вони стають
правдивішими». У приладі зашиті числа ДО зміни, тому він і скаржиться:

| перевірка | очікує | тепер |
|---|---|---|
| `count_by_state_on_date` | 10 / 1 чернетка | 9 / 2 |
| `count_by_state_period` | 120 / 9 | 118 / 11 |
| `list_by_state` | 130 / 10 | 128 / 12 |
| `unconfirmed_count` | docs=16; n=112 | docs=18; n=132 |
| `review_queue_count` | new_person=134 | new_person=4 |

Плюс `absent_breakdown_on_date`, `count_by_reason`, `with_travel_document`,
`drafts_list` — той самий механізм.

Зверни увагу: `unconfirmed_count` виріс рівно на 20 фактів, тобто на те, що я
перевів. І `+2` у чернетках — це ті самі Крижанівський і Швайко, які раніше
входили в підтверджені.

**Ці очікування треба оновити на твоєму боці.** Я не чіпаю твій прилад.

### 4 — це моє, і не на твоє прохання

| перевірка | очікує | тепер |
|---|---|---|
| `normative_list` | 41 | 44 |
| `documents_count` | normative=41 | normative=44 |
| `count_by_doc_type` | (без типу)=42 | 45 |
| `failed_docs_count` | total=201 | total=204 |

Це мої три внутрішні інструкції 251/252/253. Я залив їх **напряму з `.md`
файлів стенда Колі**, тому у виході пайплайна їх немає — і твоя звірка чесно
показує розбіжність.

Навіщо вони: у корпусі 41 документ із двох джерел (zakon.rada і tzi.com.ua), і
жанру «внутрішня інструкція підрозділу» не було **жодного**. А саме він
переважатиме в реальній частині. Заміряно: після їхньої заливки питання «за
скільки днів подавати рапорт» уперше дало правильну відповідь із першого місця
— до того такої норми в корпусі не було взагалі.

**Три варіанти, вибір за тобою:**

1. Твій прилад виключає їх за стабільним ключем —
   `pipeline_meta ->> 'loaded_by' = 'andriy_test_informal'`. Я поставив цю
   мітку саме для цього.
2. Я їх прибираю (`load_informal_test.py --remove`), і корпус повертається до
   41 документа без жанру внутрішніх інструкцій.
3. Вони проходять через пайплайн нормально, і тоді з'являються у виході — але
   це вже твоя зона, тому я цього не робив.

Я б робив (1): вони корисні, і мітка для цього вже є.

---

## Чого я не робив

* **Не чіпав документ 245** (справжній відпускний квиток) і **251–253** —
  як ти й просила.
* **Не змінював твій `verify_catalog`** — очікувані числа там твої.
* **Не виправляв другий дефект у `insert_fact`**, хоч він у тій самій функції:
  вона припускає, що новий факт хронологічно новіший, і падає на `facts_check`,
  коли приходять історичні дані (дата зарахування зі штатки старіша за
  `valid_from` наявного факту). Обійшов правилом «штатка не перезаписує
  документи», але сам дефект лишається. Це окрема робота, і вона не в цих
  трьох завданнях.
