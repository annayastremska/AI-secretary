# Сервер із H100: що це і як ним користуватись

Написано 22.08.2026 для всієї команди, простою мовою. Технічний рунбук
розгортання — окремо, `docs/deploy-gpu-server.md`.

## Що нам дали

Комп'ютер у датацентрі, який працює постійно. Відрізняється від наших
ноутбуків одним: у ньому стоїть **H100** — відеокарта, зроблена саме для
запуску моделей. Наша MamayLM робить на ній ту саму роботу, але швидко.

Навіщо це нам: локально повний цикл «фото документа → факти в базі» триває
хвилини. На демо перед комісією це виглядало б як зависання. На сервері та
сама робота триває секунди.

Сервер обіцяний **до 31 серпня** — тобто це майданчик для демо, а не нове
місце життя проєкту. Пайплайн і далі мусить працювати на ноуті.

## Дві «двері» в сервер, і чому їх дві

Уяви бізнес-центр, у якому багато офісів. Наш офіс — один із них, і в нього
два входи:

**Двері №1 — порт 7301 — «сісти за клавіатуру сервера».**
Це SSH: ти під'єднуєшся й отримуєш командний рядок ТОГО комп'ютера. Так
встановлюють програми, запускають нашу апку, дивляться логи. Потрібен
файл-ключ (див. нижче) — без нього двері не відчиняються.

**Двері №2 — порт 7302 — «зайти на наш сайт».**
Це звичайний браузер: відкриваєш `http://185.9.41.1:7302` і бачиш нашу
веб-апку — сторінку завантаження документа й чат. Ніякого ключа не треба,
достатньо посилання.

**Чому саме числа 7301 і 7302.** Один сервер обслуговує кілька команд, і щоб
запити не змішувались, кожній видали власні номери. Це як номер квартири в
будинку з однією адресою.

**Чому в інструкції зустрічається ще й порт 80.** Це «внутрішній номер
кабінету». Наша апка всередині сервера слухає 80, а зовні до неї стукають на
7302, і сервер сам перекидає стук усередину. Тобто **80 — це те, що
налаштовуємо ми; 7302 — те, що бачать люди**. Плутанина тут найчастіша.

## Як подивитись своїми очима — просто зараз

Відкрий у браузері `http://185.9.41.1:7302`.

**Станом на 22.08 там нічого немає, і це очікувано:** ми перевірили обидві
двері — SSH (7301) відповідає, тобто сервер живий; HTTP (7302) не відповідає,
бо апку туди ще не поставили. Двері є, за ними поки порожня кімната.

Коли апку розгорнемо, за тим самим посиланням з'явиться:
- `/` — завантажити документ і побачити, що з нього витягнулось;
- `/chat` — поставити питання до бази.

Тобто **нікому з команди не потрібно нічого встановлювати**, щоб користуватись
демо: браузер і посилання. Ключ і командний рядок потрібні лише тому, хто
розгортає.

## Що потрібно, щоб ми почали

Один файл — приватний ключ до SSH (той, що згадується в повідомленні як
`<KEY_FILE>`). Без нього під'єднатись неможливо: сервер навмисно не пускає
за паролем, тільки за ключем.

**Важливо про ключ:** це не пароль, який можна переслати в чат і забути. Це
файл, який дає повний доступ до машини. Правило просте: він лежить локально
у людини, яка розгортає, і ніколи не потрапляє ні в git, ні в спільний чат.

## Одне обмеження, яке не обговорюється

Сервер **чужий і спільний**, а двері №2 (7302) відкриті в інтернет — за
посиланням може зайти будь-хто, хто його знає.

Тому: **на сервер їдуть лише синтетичні документи.** Жодного реального
документа частини, жодних реальних даних у базі. Для демо синтетики
достатньо — вона в нас уже є й перевірена.

Це не перестороженість: у нашій апці немає поняття користувача (немає логінів
і ролей), тобто хто відкрив посилання — той усе й бачить.

## Правила спільної роботи: чому ми лишили один акаунт

**Що ми маємо.** Заходимо всі одним акаунтом `ubuntu`, персональних немає. У
списку дозволених ключів лежить **один** ключ провайдера — тобто вся команда
користується тим самим приватним ключем, який ходить по чату.

**Що радить індустрія.** Окремий акаунт на людину: спільний стирає
відповідальність (не видно, хто що зробив), ускладнює відкликання доступу й
збільшує втрати, якщо ключ утік
([NCSC](https://www.ncsc.gov.uk/collection/using-online-services-safely/creating-separate-user-accounts),
[UTK](https://oit.utk.edu/security/learning-library/article-archive/avoiding-shared-accounts/)).

**Що вирішили ми і чому інакше.** Сервер живе 9 днів. Окремі акаунти дають
ізоляцію файлів і точний аудит — на демо-стенді це майже не потрібно, а коштує
координації. Реальні ризики тут інші: затерти одне одному роботу, дві апки на
одному порту, забитий диск, ключ, який неможливо відкликати поштучно. Тому:

**1. Кожен додає СВІЙ ключ** у той самий акаунт, окремим рядком із підписом.

Процедуру перевірено на живому сервері 22.08 (створили тимчасовий ключ, додали,
зайшли, побачили в логах, прибрали — усі п'ять кроків справді працюють).

### Крок 1 — згенерувати собі ключ (у себе на машині)

```bash
ssh-keygen -t ed25519 -f ~/.ssh/kse-h100-mine -C "anya-laptop" -N ""
```

`-C` — підпис, за яким тебе буде видно в логах. Пиши ім'я + машину
(`anya-laptop`, `andriy-pc`), а не «key1». `-N ""` — без пароля на ключ; якщо
хочеш пароль, прибери цей параметр.

З'явиться **два** файли: `kse-h100-mine` (приватний — нікому, ніколи, ні в git,
ні в чат) і `kse-h100-mine.pub` (публічний — його й надсилаєш).

### Крок 2 — надіслати публічну частину

```bash
cat ~/.ssh/kse-h100-mine.pub
```

Один рядок, починається на `ssh-ed25519`. Його можна спокійно кидати в чат —
він для цього створений.

### Крок 3 — додати на сервер (робить той, у кого вже є доступ)

```bash
# спершу резервна копія -- зіпсований файл = ніхто не зайде
cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.bak

# додаємо ОДНИМ рядком, обов'язково >> (додати), а не > (перезаписати!)
echo "ssh-ed25519 AAAA...=== anya-laptop" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Помилка з `>` замість `>>` затирає всі ключі й закриває доступ усім — тому
резервна копія першим кроком, а не «як буде час».

### Крок 4 — перевірити, що працює

```bash
ssh -p 7301 -i ~/.ssh/kse-h100-mine ubuntu@185.9.41.1 "whoami"
```

Має відповісти `ubuntu`. Якщо `Permission denied` — рядок додався неправильно
(найчастіше розірваний на два).

### Крок 5 — переконатись, що в логах видно, ЧИЙ це вхід

```bash
# хто які ключі має (відпечаток -> підпис)
ssh-keygen -lf ~/.ssh/authorized_keys

# хто заходив останнім
sudo grep 'Accepted publickey' /var/log/auth.log | tail -5
```

У логу видно відпечаток (`SHA256:NJjdd...`), у першій команді — той самий
відпечаток поруч із підписом. Зіставляєш одне з одним і бачиш людину. Саме це
й замінює нам окремі акаунти.

### Якщо ключ треба відкликати (загубився ноутбук, людина пішла)

```bash
grep -v "anya-laptop" ~/.ssh/authorized_keys > /tmp/ak && mv /tmp/ak ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Перевірено: після цього той ключ одразу отримує `Permission denied`, а решта
працює далі. Це і є та вигода, якої немає при спільному ключі — там довелося б
міняти доступ усім одночасно.

**2. Домовленість, яку технікою не заміниш** (лежить на сервері як
`~/README-домовленості.md`):

| Правило | Чому |
|---|---|
| працюй у своїй папці: `~/anya`, `~/andriy`, `~/kolya`, `~/denys` | інакше двоє склонують репо в ту саму папку й затруть одне одного |
| апку на порту 80 запускає **одна** людина | дві на одному порту не влізуть; перевірити: `ss -tln \| grep :80` |
| ваги моделей — у спільній `~/shared/models` | 9+ ГБ; диск 48 ГБ, вільно 32 — три копії його забʼють |
| довгі процеси — у tmux з іменем: `tmux new -s anya-app` | інший побачить `tmux ls` і не вбʼє чужий прогін |

## Що вже зроблено і що робимо далі

**Зроблено (22.08):**
- перевірено доступ: SSH працює, машина `document-management`, **H100 на 80 ГБ**,
  188 ГБ оперативної памʼяті, диск 48 ГБ (вільно 32);
- перевірено, що порт 80 **вільний** — апці є куди стати;
- створено `~/anya`, спільну `~/shared/models` і `~/README-домовленості.md`;
- код пайплайна вже адаптований під GPU (профіль, CUDA-залежності, хост/порт
  через оточення) — `docs/deploy-gpu-server.md`.

**Найближче:**
1. поставити docker (його **немає**, а на ньому підіймається база — Postgres і
   MinIO, тобто без нього чат не має звідки брати цифри);
2. поставити залежності й **завантажити ваги** MamayLM у спільну папку;
3. розгорнути апку на порт 80 → перевірити, що ззовні відповідає `:7302`;
4. прогнати демо-набір документів через пайплайн і наповнити базу;
5. заміряти швидкість на GPU і порівняти з ноутбуком (12B, потім 27B).

**Не забути:** сервер обіцяний **до 31.08**. Усе, що там лежить, після цього
зникає — тому єдине джерело істини лишається git, а не сервер.

## Коротко, одним абзацом

Нам дали потужний комп'ютер у мережі. У нього два входи: 7301 — для того,
хто налаштовує (потрібен ключ-файл), 7302 — для всіх інших, щоб просто
подивитись у браузері. Усередині апка живе на порту 80, зовні її видно як
7302. Сервер уже працює, апки на ньому ще немає. Реальні документи туди не
кладемо.
