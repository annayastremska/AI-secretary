# Сервер із H100: що це і як ним користуватись

Написано 22.08.2026 для всієї команди, простою мовою; дописано **24.08** —
розділ «Відеокарта перестала рахувати» і свіжий стан робіт. Технічний рунбук
розгортання — окремо, `docs/deploy-gpu-server.md`.

**Якщо читати один рядок:** сервер працює, демо-набір прогнаний і лежить готовий
до завантаження в базу, але **відеокарта зараз не рахує** — немає ліцензії. Ми
обійшли це процесором: повільніше, якість не гірша. Демо не під загрозою.

## Що нам дали

Комп'ютер у датацентрі, який працює постійно. Відрізняється від наших
ноутбуків одним: у ньому стоїть **H100** — відеокарта, зроблена саме для
запуску моделей. Наша MamayLM робить на ній ту саму роботу, але швидко.

Навіщо це нам: локально повний цикл «фото документа → факти в базі» триває
хвилини. На демо перед комісією це виглядало б як зависання. На сервері та
сама робота триває секунди.

**Це «мусило б тривати секунди».** З 23.08 карта не рахує — немає ліцензії, —
тому зараз ми працюємо на процесорі: фото 47 с замість ~23 с. Чому так і що
просити, щоб виправити, — розділ «Відеокарта перестала рахувати».

Сервер обіцяний **до 31 серпня** — тобто це майданчик для демо, а не нове
місце життя проєкту. Пайплайн і далі мусить працювати на ноуті.

## Дві «двері» в сервер, і чому їх дві

Уяви бізнес-центр, у якому багато офісів. Наш офіс — один із них, і в нього
два входи:

**Двері №1 — порт 7301 — «сісти за клавіатуру сервера».**
Це SSH: ти під'єднуєшся й отримуєш командний рядок ТОГО комп'ютера. Так
встановлюють програми, запускають нашу апку, дивляться логи. Потрібен
файл-ключ (див. нижче) — без нього двері не відчиняються.

**Двері №2 — порт 7302 — «зайти на наш сайт».**
Це звичайний браузер: відкриваєш `http://185.9.41.1:7302` і бачиш нашу
веб-апку — сторінку завантаження документа й чат. Ніякого ключа не треба,
достатньо посилання.

**Чому саме числа 7301 і 7302.** Один сервер обслуговує кілька команд, і щоб
запити не змішувались, кожній видали власні номери. Це як номер квартири в
будинку з однією адресою.

**Чому в інструкції зустрічається ще й порт 80.** Це «внутрішній номер
кабінету». Наша апка всередині сервера слухає 80, а зовні до неї стукають на
7302, і сервер сам перекидає стук усередину. Тобто **80 — це те, що
налаштовуємо ми; 7302 — те, що бачать люди**. Плутанина тут найчастіша.

## Як подивитись своїми очима — просто зараз

Відкрий у браузері `http://185.9.41.1:7302`.

**Станом на 22.08 там нічого немає, і це очікувано:** ми перевірили обидві
двері — SSH (7301) відповідає, тобто сервер живий; HTTP (7302) не відповідає,
бо апку туди ще не поставили. Двері є, за ними поки порожня кімната.

Коли апку розгорнемо, за тим самим посиланням з'явиться:
- `/` — завантажити документ і побачити, що з нього витягнулось;
- `/chat` — поставити питання до бази.

Тобто **нікому з команди не потрібно нічого встановлювати**, щоб користуватись
демо: браузер і посилання. Ключ і командний рядок потрібні лише тому, хто
розгортає.

## Що потрібно, щоб ми почали

Один файл — приватний ключ до SSH (той, що згадується в повідомленні як
`<KEY_FILE>`). Без нього під'єднатись неможливо: сервер навмисно не пускає
за паролем, тільки за ключем.

**Важливо про ключ:** це не пароль, який можна переслати в чат і забути. Це
файл, який дає повний доступ до машини. Правило просте: він лежить локально
у людини, яка розгортає, і ніколи не потрапляє ні в git, ні в спільний чат.

P.S (пише Аня, не клод) я його поклала в C:\Users\<ваш_юзер>\.ssh\kse-h100

## Відеокарта перестала рахувати (24.08) — і що ми з цим зробили

### Навіщо нам відеокарта взагалі

Дві найважчі речі в пайплайні люблять саме її:

| що | навіщо |
|---|---|
| **розпізнавання тексту** (OCR) | прочитати документ із **фотографії**. `.docx` ми читаємо просто так, а знімок телефоном треба «розібрати очима» |
| **мовна модель** (MamayLM 27B) | здогадатись про поле, яке не вдалось прочитати за версткою бланка |

Обидві — мільярди дрібних множень. Процесор робить їх по черзі, відеокарта —
тисячами одночасно. Тому на карті це секунди, на процесорі — десятки секунд.

### У чому проблема

Наша «відеокарта» — не окрема залізяка. Це **нарізка** однієї великої карти
(H100) на кілька віртуальних, і одна нарізка дісталась нам.

Головне, чого ми не знали: **нарізка вимагає ліцензії.** Не карта — саме
нарізка. Без ліцензії драйвер сам себе обмежує: карту видно, пам'ять видно, а
рахувати вона відмовляється.

```
License Status : Unlicensed          ← ліцензії немає

22.08 11:30   GPU features and performance are restricted        ← обмежено
23.08 11:10   GPU features and performance are fully restricted  ← обмежено ПОВНІСТЮ
```

Тобто ліцензії не було **ніколи**. Спершу діяв пільговий період — карта
працювала неповноцінно, але працювала, і наші прогони 22–23 серпня встигли
пройти саме в цьому вікні. Потім пільга закінчилась.

**Це не наша поломка й не поломка коду:** будь-яка програма отримує ту саму
відмову «операція не підтримується». Перевірено двома різними, не нашими.

### Що сервер дозволяє зараз, а що ні

| | працює? | швидкість |
|---|---|---|
| читати `.docx` і `.pdf` | **так** | секунди, як і було — карта тут не потрібна |
| розпізнавати фото (OCR) | **так, на процесорі** | 47 с на знімок замість ~23 с на карті |
| мовна модель | **так, на процесорі** | 43 с на звернення замість секунд |
| використати саму карту | **ні** | — |
| база, пошук, чат | **так** | карта їм не потрібна взагалі |

**Нічого з нашої роботи не заблоковано.** Дорожче стало лише те, що торкається
фото й моделі.

### Як обійшли — і де була наша власна недоробка

Обхід із двох кроків, і другий вартий уваги всіх.

**Перший:** сказати програмам «карти немає». Звучить дивно, але в цьому й суть:
коли програма бачить карту, вона намагається нею скористатись і падає; коли
карти «немає» — спокійно рахує на процесорі. Один рядок налаштування.

**Другий — наша недоробка.** Програма розпізнавання вміє двома способами: один
вимагає карти, другий обходиться процесором. Вибирає вона сама, і вибирає так:

> «Чи є в системі відеокарта? Якщо є — беру спосіб для карти.»

А карта **є**. Її видно в списку. Вона просто не рахує. Тому програма щоразу
брала спосіб для карти, десять хвилин чекала й віддавала «не змогла прочитати
документ».

**«Карта є» і «карта працює» — різні речі, і вгадувати друге за першим не
можна.** Ми цей вибір ніколи не закріплювали — покладались на автовизначення.
Тепер спосіб задається явно (`demos/upload_app/config-cpu.yaml`), і саме тому
фото обробились.

### Чим це коштувало

Якість **не просіла**, а на поганому знімку навіть зросла:

| знімок | на процесорі | було на карті |
|---|---|---|
| три звичайні кадри | **46 полів із 46** | 44 з 44 |
| навмисно розмитий кадр | **7 полів із 14** | 5 з 14 |

Заплатили лише часом: чотири знімки — 4 хвилини 53 секунди.

### Що просити і в кого

У тих, хто видавав віртуалку (адміністратори курсу / інфраструктури). Абзац
нижче можна переслати **дослівно**:

> Віртуальна машина `185.9.41.1` з профілем **NVIDIA H100-80C**
> (NVIDIA Virtual Compute Server) — **vGPU не ліцензований**: `nvidia-smi -q`
> показує `License Status: Unlicensed`, у журналі `nvidia-gridd` —
> `fully restricted`, CUDA-обчислення недоступні
> (`CUDA error: operation not supported`). Потрібен **client configuration
> token** (`.tok`) з вашого NVIDIA License System: покласти у
> `/etc/nvidia/ClientConfigToken/` і зробити `systemctl restart nvidia-gridd`.
> Зараз ця папка порожня, а в `/etc/nvidia/gridd.conf` не заданий сервер
> ліцензій (`ServerAddress=` порожній, `FeatureType=0` — для Virtual Compute
> Server має бути `4`).

Що ми перевірили самі, щоб не витрачати їхній час: токена немає ніде на машині,
сервер ліцензій не заданий, годинник синхронізований (розсинхрон теж дає
«Unlicensed» — але не наш випадок).

### Що це означає для демо

1. **Живий показ обробки фото — це ~47 секунд**, а не ~20. Або показуємо
   `.docx` (секунди), або чесно кажемо «зараз без відеокарти».
2. **Якщо ліцензію дадуть до демо** — перемикаємо один рядок налаштування, і все
   стає швидшим. Переробляти нічого не треба.

## Одне обмеження, яке не обговорюється

Сервер **чужий і спільний**, а двері №2 (7302) відкриті в інтернет — за
посиланням може зайти будь-хто, хто його знає.

Тому: **на сервер їдуть лише синтетичні документи.** Жодного реального
документа частини, жодних реальних даних у базі. Для демо синтетики
достатньо — вона в нас уже є й перевірена.

Це не перестороженість: у нашій апці немає поняття користувача (немає логінів
і ролей), тобто хто відкрив посилання — той усе й бачить.

## Правила спільної роботи: чому ми лишили один акаунт

**Що ми маємо.** Заходимо всі одним акаунтом `ubuntu`, персональних немає. У
списку дозволених ключів лежить **один** ключ провайдера — тобто вся команда
користується тим самим приватним ключем, який ходить по чату.

**Що радить індустрія.** Окремий акаунт на людину: спільний стирає
відповідальність (не видно, хто що зробив), ускладнює відкликання доступу й
збільшує втрати, якщо ключ утік
([NCSC](https://www.ncsc.gov.uk/collection/using-online-services-safely/creating-separate-user-accounts),
[UTK](https://oit.utk.edu/security/learning-library/article-archive/avoiding-shared-accounts/)).

**Що вирішили ми і чому інакше.** Сервер живе 9 днів. Окремі акаунти дають
ізоляцію файлів і точний аудит — на демо-стенді це майже не потрібно, а коштує
координації. Реальні ризики тут інші: затерти одне одному роботу, дві апки на
одному порту, забитий диск, ключ, який неможливо відкликати поштучно. Тому:

**1. Кожен додає СВІЙ ключ** у той самий акаунт, окремим рядком із підписом.

Процедуру перевірено на живому сервері 22.08 (створили тимчасовий ключ, додали,
зайшли, побачили в логах, прибрали — усі п'ять кроків справді працюють).

### Крок 1 — згенерувати собі ключ (у себе на машині)

```bash
ssh-keygen -t ed25519 -f ~/.ssh/kse-h100-mine -C "anya-laptop" -N ""
```

`-C` — підпис, за яким тебе буде видно в логах. Пиши ім'я + машину
(`anya-laptop`, `andriy-pc`), а не «key1». `-N ""` — без пароля на ключ; якщо
хочеш пароль, прибери цей параметр.

З'явиться **два** файли: `kse-h100-mine` (приватний — нікому, ніколи, ні в git,
ні в чат) і `kse-h100-mine.pub` (публічний — його й надсилаєш).

### Крок 2 — надіслати публічну частину

```bash
cat ~/.ssh/kse-h100-mine.pub
```

Один рядок, починається на `ssh-ed25519`. Його можна спокійно кидати в чат —
він для цього створений.

### Крок 3 — додати на сервер (робить той, у кого вже є доступ)

```bash
# спершу резервна копія -- зіпсований файл = ніхто не зайде
cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.bak

# додаємо ОДНИМ рядком, обов'язково >> (додати), а не > (перезаписати!)
echo "ssh-ed25519 AAAA...=== anya-laptop" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Помилка з `>` замість `>>` затирає всі ключі й закриває доступ усім — тому
резервна копія першим кроком, а не «як буде час».

### Крок 4 — перевірити, що працює

```bash
ssh -p 7301 -i ~/.ssh/kse-h100-mine ubuntu@185.9.41.1 "whoami"
```

Має відповісти `ubuntu`. Якщо `Permission denied` — рядок додався неправильно
(найчастіше розірваний на два).

### Крок 5 — переконатись, що в логах видно, ЧИЙ це вхід

```bash
# хто які ключі має (відпечаток -> підпис)
ssh-keygen -lf ~/.ssh/authorized_keys

# хто заходив останнім
sudo grep 'Accepted publickey' /var/log/auth.log | tail -5
```

У логу видно відпечаток (`SHA256:NJjdd...`), у першій команді — той самий
відпечаток поруч із підписом. Зіставляєш одне з одним і бачиш людину. Саме це
й замінює нам окремі акаунти.

### Якщо ключ треба відкликати (загубився ноутбук, людина пішла)

```bash
grep -v "anya-laptop" ~/.ssh/authorized_keys > /tmp/ak && mv /tmp/ak ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Перевірено: після цього той ключ одразу отримує `Permission denied`, а решта
працює далі. Це і є та вигода, якої немає при спільному ключі — там довелося б
міняти доступ усім одночасно.

**2. Домовленість, яку технікою не заміниш** (лежить на сервері як
`~/README-домовленості.md`):

| Правило | Чому |
|---|---|
| працюй у своїй папці: `~/anya`, `~/andriy`, `~/kolya`, `~/denys` | інакше двоє склонують репо в ту саму папку й затруть одне одного |
| апку на порту 80 запускає **одна** людина | дві на одному порту не влізуть; перевірити: `ss -tln \| grep :80` |
| ваги моделей — у спільній `~/shared/models` | 9+ ГБ; диск 48 ГБ, вільно 32 — три копії його забʼють |
| довгі процеси — у tmux з іменем: `tmux new -s anya-app` | інший побачить `tmux ls` і не вбʼє чужий прогін |

## Що вже зроблено, що лежить на сервері і чого воно чекає

Станом на **24.08**.

### Зроблено

**Сервер піднятий:**
- SSH працює; машина `document-management`, H100 на 80 ГБ, 188 ГБ оперативної
  памʼяті;
- створено `~/anya`, спільну `~/shared/models` і `~/README-домовленості.md`
  (правила спільного акаунта);
- **ваги MamayLM v2 27B** лежать у `~/shared/models` (16,5 ГБ) — одна копія на
  всіх, другої диск не витримає;
- docker поставлений, образи витягнуті: **vllm** (для розпізнавання) і
  **pgvector/pgvector:pg16** (база — Андрію не треба тягнути свій);
- код пайплайна адаптований: профілі під сервер, CUDA-залежності, хост і порт
  через оточення.

**Демо-набір прогнаний — і це головне:**
- **198 документів із 198**: 17 історія `.docx` + 5 тих самих у `.pdf` + 4 фото
  + 130 масовки + 42 нормативні;
- результат лежить у `~/anya/ai-secretary/data/output-demo/` — **198 файлів
  `.md`**, готових до завантаження в базу;
- це **один** повний прогін: усі попередні тестові виходи прибрані, щоб не було
  питання «а це яка версія»;
- жодного документа зі статусом «не змогла нічого сказати»;
- 176 записів підтверджені, 22 — чернетки (у них є хоч одне непевне поле, і за
  правилом проєкту вони не входять у підрахунки, доки людина не підтвердить).

**Перевірено, а не заявлено:**
- усі 426 текстових значень, які система вважає прочитаними з бланка, **дослівно
  є в документах** — не вибірка з десяти, а весь прогін;
- повторний прогін дає **побайтово ті самі записи** — цифри відтворюються;
- усі 198 файлів читаються машиною без помилок (окрема перевірка після того, як
  один зіпсований запис виявив баг у нашому форматі — уже виправлений).

### Чого це чекає

| чекає на | від кого | що блокує |
|---|---|---|
| **завантажити 198 записів у базу** | Андрій | нічого — усе готове, потрібна одна команда |
| **одна правка в завантажувачі** (читати «модель чинності» з факту, а не зі своєї копії) | Андрій | без неї три виміри поїдуть у базу з неправильною ознакою, і **мовчки** |
| **не показувати той самий документ двічі** | Андрій | 8 документів є і в `.docx`, і в `.pdf` — інакше на демо підрахунок і список дадуть різні числа |
| **відповідь: хто вантажить нормативку** | Андрій | зараз два незалежні шляхи, і другий тихо перезаписує перший |
| **ліцензія на відеокарту** | адміністратори | нічого критичного; лише швидкість фото (див. розділ вище) |
| **429 ГБ диска** (є, але не підключені) | хто зайде першим | нічого зараз — але запас поменшав: вільно 18 ГБ із 96 |

Усе, що потрібно Андрію, зібрано в одному файлі з чек-лістом:
**`docs/contracts/2026-08-23_instruction-for-andriy.md`**.

### Чого на сервері свідомо НЕМА

- **чат і апка** — вони ще в розробці, тому на сервер не ставили;
- **три файли `live/`** — резерв для живого показу: вони мусять лишитись
  невідомими базі, інакше «подивіться, система бачить документ уперше» буде
  неправдою;
- **реальні документи частини** — і це не обговорюється, див. розділ нижче.

**Не забути:** сервер обіцяний **до 31.08**. Усе, що там лежить, після цього
зникає — тому єдине джерело істини лишається git, а не сервер.

## Коротко, одним абзацом

Нам дали потужний комп'ютер у мережі. У нього два входи: 7301 — для того,
хто налаштовує (потрібен ключ-файл), 7302 — для всіх інших, щоб просто
подивитись у браузері. Усередині апка живе на порту 80, зовні її видно як 7302.

Станом на 24.08: сервер працює, **демо-набір повністю прогнаний — 198 записів
лежать готові до завантаження в базу**, апки на сервері ще немає (вона в
розробці). Відеокарта не рахує через відсутню ліцензію — обійшли процесором,
якість не постраждала. Реальні документи туди не кладемо.
