# Критерії приймання по блоках харнесу + перевірка на побічну шкоду

_27.08.2026, Аня. Супровід до `2026-08-27_harness-research-plan.md`. Правило,
задане Анею: **кожна зміна перевіряється не лише на своїй проблемі, а й на
негативний вплив в інших ділянках**, і між блоками стоїть загальна перевірка._

---

## 0. Як цим користуватись

На кожен блок три речі, і всі три обов'язкові:

| | що це | без цього не приймається |
|---|---|---|
| **К** | критерій: число або траса, які закривають блок | інакше «здається краще» |
| **Ш** | щит блоку: що саме ця правка може зламати **поза** блоком | інакше ламаємо тихо |
| **З** | загальна перевірка перед переходом до наступного блоку | інакше поломка знайдеться через три блоки |

Порядок роботи над блоком незмінний: **щит → правка → К → Ш → З → наступний
блок.** Правка перед щитом — заборонена; це та сама помилка, що з `tail -5` у
плані apt: перевірка, зроблена після дії, не є запобіжником.

---

## 1. Загальна перевірка (З) — однакова для всіх блоків

Це шість запусків, і **два з них уже ловили мої власні поломки**, тому список
не скорочується під приводом «ця правка дрібна».

```bash
# 1. увесь набір тестів апки (локально, бази не треба)
python -m pytest demos/upload_app/tests -q

# 2. щит Дениса окремо й уголос -- щоб бачити його число, а не «все зелено»
python -m pytest demos/upload_app/tests/test_denys_shield.py -q

# 3. п'ять базових цифр пайплайна: ті самі до і після
python eval/check_all.py

# 4. жива звірка виходу з базою (на сервері, читання)
python demos/upload_app/verify_catalog.py --output-dir data/output-demo

# 5. дим по стику чата з базою (на сервері, читання)
python demos/upload_app/verify_stack.py

# 6. правила на живих ходах: джерело в кожній відповіді, нуль збоїв
python demos/upload_app/trace_lookup.py --check
```

**Що вважається провалом З:**

* будь-який тест, що був зеленим, став червоним — **навіть якщо «тест
  неправильний»**. Тоді спершу записується, чому очікування змінилось, і лише
  потім рухається тест;
* `verify_catalog` дав менше, ніж було (зараз 34/34);
* `check_all.py` змінив хоч одну з п'яти цифр без явного пояснення;
* `trace_lookup --check` знайшов відповідь без джерела або технічний збій.

**Окремо про `xfail`:** у щиті є один `xfail(strict=True)` — недійсна ISO-дата
(п. 23). Коли його полагодять, тест **стане червоним** і скаже зняти мітку. Це
не поломка, це спрацював запобіжник.

---

## 2. Блок C — ідентифікація особи (перший за пріоритетом)

**К. Критерій — три числа окремо, на `eval/chat/identity.tsv`:**

| число | приймання |
|---|---|
| та сама особа (`object_id`, не рядок) | 100% або відмова. «Інша людина впевнено» — **нуль** |
| чистота картки: усі документи належать цій особі | **нуль чужих документів. Критерій-стоп** |
| виправлення вводу назване вголос | тихих виправлень **нуль** |

«Не знайшла» тут — успіх, а не провал. Прилад, який міряє «знайшлась людина»,
показав би 100% на всіх п'яти провалах Дениса.

**Ш. Що ця правка може зламати поза блоком C:**

1. **п. 30 — відмінки довгих прізвищ.** «Крижанівського» → у коді стоїть
   обрізання на **три** літери саме через цей випадок, і в комментарі написано:
   без нього чат «впевнено заперечував власні дані». Прибрати обрізання —
   зламати це. Перевірка: живий прогін «Що відомо про Крижанівського?» і
   «Розкажи все про Ґоляша».
2. **п. 24 — латиниця й одруківки.** Правка «зробити пошук терплячішим» — це
   найкоротший шлях почати вгадувати `Malyshko` і «Голяш». Перевірка: щит,
   `test_latin_surname_is_not_guessed`.
3. **Дві копії функції.** `extract_name` існує в `app.py` **і** в `tiers.py`, з
   різною глибиною обрізання (3 і 2) і різними джерелами. Полагодити одну —
   полагодити половину. Перевірка: щит,
   `test_two_copies_of_name_search_exist_and_must_be_fixed_together`.
4. **Три входи в базу на одну задачу.** `tiers._run_template_sql`,
   `db.find_people`, `db.absences_for_person`. Знайдено при написанні щита: перша
   версія фікстури патчила один вхід, і тест пішов у живу базу.
5. **`STOP_CAPS` неповний**: «Дякую», «Тільки», «Добре» їдуть у базу як
   кандидати в прізвища. Сьогодні невидимо (нічого не знаходять), після правки
   стане видимим одразу. Перевірка: щит,
   `test_polite_words_are_not_sent_to_the_database_as_surnames` — він рахує
   звернення й не дає їх **збільшити**.

---

## 3. Блок B — пам'ять наступного ходу

**К.** Жива траса, не прилад: «Скільком у відпустці 2026-10-10?» → «А у
відрядженні?» мусить дати **відповідь**, і в ній **видимий рядок** «дату взято з
попереднього питання». Друга траса: «Хто у відпустці в 3 роті?» → «А у взводі
забезпечення?». Плюс число Андрія 18/21 підтверджене на приладі після
розгортання (три інстанси, бо `llama.cpp` при `temperature=0` не відтворюваний).

**Ш.**

1. **Хибне успадкування** — шкода від пам'яті, а не її брак, і важить більше за
   приріст. Обов'язкова траса зміни теми: «Скільком у відпустці 10.10?» → «За
   скільки днів подавати рапорт?» — дата **не** має успадкуватись. У Андрія на
   правці промпту 0 з 5, на правці каталогу 5 з 5. Тобто це реально ламається.
2. **п. 30** — «А хто саме?» не має ставати прізвищем. Щит, той самий тест.
3. **Каталог**: правка заголовків торкається `query_catalog.yaml`, а на ньому
   стоять `verify_catalog` (34 перевірки) і `test_router_testset_matches_catalog`.
4. **Відтворюваність.** Успадкування ламає її за визначенням, тому прилад мусить
   міряти обидва режими (з історією й без).

---

## 4. Блок E — одиниця обліку й знаменник

### 4а. Додано 28.08 на вимогу Ані: «скільком» мусить давати ще й ПЕРЕЛІК

> «модель має повертати не тільки число, а і перелік людей по пунктах у таких
> ситуаціях»

Це та сама родина, що й решта блоку E: людині потрібна не цифра, а можливість
її перевірити. «1 особа у відпустці» без прізвища перевірити неможливо —
секретареві треба знати, ХТО, щоб подзвонити. У беклозі чата це вже стояло
окремим болем («розклад по підрозділах не називає імен, коли особа одна»).

**Критерій:** відповідь на «скільком…» містить перелік осіб, поки їх небагато.
Поріг обов'язковий: у коді вже стоїть свідома відмова друкувати кількасот
прізвищ («переліку не даю навмисно: кількасот прізвищ — це не відповідь»), і
її ламати не можна. Тобто:

* n ≤ порогу → число **і** перелік по пунктах, із документом-джерелом у рядку;
* n > порогу → число, і **сказано**, що перелік не показано й як його
  отримати («покажи поіменно»).

**Ш (побічна шкода):** перелік бере дані іншим запитом (`list_by_state`), ніж
число (`count_by_state_on_date`), а це рівно те місце, де в п. 12 звіту
розійшлись «9 людей» і «10 рядків». Тому обов'язкова перевірка: **кількість
рядків переліку = числу**, інакше відповідь суперечить сама собі на одному
екрані. Це вже закладено в каталозі (дедуплікація docx/pdf), і саме це треба
підтвердити заміром, а не вірити.

**К.** `measure_counts.py`: одне питання всіма дорогами, що на нього
відповідають. Приймання не «числа однакові», а точніше: **числа різняться лише
там, де у відповіді явно названа інша метрика або інший знаменник.** 12 і 15 на
одну дату законно, якщо написано «у відпустці» і «поза частиною = відпустка +
відрядження»; незаконно, якщо не написано.

Плюс п. 21: те саме питання з подвійними пробілами, капсом і неразривним
пробілом дає **байт у байт** ту саму відповідь.

**Ш.**

1. **п. 27** — тексти про «нуль означає немає даних» живуть у тому самому місці,
   де переписуються тексти про числа. Щит,
   `test_date_outside_coverage_says_no_data_not_nobody`.
2. **Сторінка статистики.** Числа чата й числа сторінки беруться з різних
   запитів; якщо я міняю знаменник у чаті, сторінка мусить сказати те саме.
   Перевірка: `test_stats_numbers_are_guarded` (точні значення) + око на сторінці.
3. **Блок D підживлює E**: №118 і №131 з одним номером подвоюють Малишко в
   переліках. Тобто частину розбіжності «люди/рядки» дасть не арифметика, а
   дублікат. Не приймати E, не перевіривши це на Малишко окремо.
4. **Нормалізація вводу** зачіпає ВСІ дороги, включно з правиловими, які тримають
   п. 23, 24, 27. Після неї — повний щит, не вибірково.

---

## 5. Блок F — «перший параметр виграє»

**К.** Чотири формулювання з п. 16 і п. 17: система або відповідає на обидві
половини, або **каже, що відповідає про одну**. Тихої відповіді на половину
питання — нуль. Плюс дрібне: текст не обривається посеред слова («в залежності
від вис»).

**Ш.** Наявний `test_tricky_questions.py` уже тримає шість випадків цієї родини
(заперечення, порівняння підрозділів, «у 2 і 3 роті разом»). Правка блоку F
переписує рівно ту дорогу — цей файл мусить лишитись зеленим без правок у ньому.
Якщо його довелось правити — це не правка F, це зміна поведінки, і її треба
назвати.

---

## 6. Блок D — «чинний»

**К.** Слово «чинний» більше не позначає `facts.status='confirmed'`. Пряме
питання «Покажи документ №118» не називає анульований документ чинним.

**Ш.** `STATUS_LABEL` читається в кількох місцях рендера; перейменування мітки
зачіпає `test_answer_product_rules`, `test_reply_presentation`,
`test_unmatched_person_visible`. Це очікувано — важливо, щоб змінились **тексти**,
а не правило «чернетка не входить у підрахунок».

---

## 7. Блок 7 (нормативка) — заблокований, і це записано окремо

Не рухається до відповіді Андрія. Причини й ризики —
`docs/research/2026-08-27_normative-two-roads.md`, розділ 6.

**Що робиться незалежно:** «звідки ти це знаєш?» (п. 18) — дорога до вже
наявного сліду ходу. **К:** питання «Звідки ти це знаєш?» після відповіді дає
дорогу, шаблон і кількість рядків людською мовою, а не той самий текст, яким
чат відмовляє на питання про погоду. **Ш:** `test_provenance_road.py` і
`trace_lookup --check` (правило «кожна відповідь має джерело»).

---

## 7б. ГЛОБАЛЬНА ПЕРЕВІРКА 28.08 — усі прилади на живій системі

| # | перевірка | результат |
|---|---|---|
| 1 | `pytest demos/upload_app/tests` | **517 passed**, 7 skipped, 0 failed |
| 2 | `verify_catalog.py` (жива база) | **35 із 35**, провалів 0 |
| 3 | `measure_identity.py` (жива база) | **15/15 особа, 15/15 картка, 15/15 ввід** |
| 4 | `measure_counts.py` (жива база + модель) | **12 із 12**, провалів 0 |
| 5 | `verify_stack.py` (дим по стику) | провалів 0; модель на карті |
| 6 | `trace_lookup.py --check` | усі 31 відповідей із джерелом, технічних збоїв 0 |
| 7 | `eval/check_all.py` (базова лінія) | **погіршень немає** |
| 8 | живі траси через `answer()` | **12 із 13** |

### Що знайшла сама глобальна перевірка (і чого не бачили блокові)

**п. 7 звіту жив ще ПІСЛЯ блоку C — і мій же щит про це попереджав.**
Живі траси показали, що «Де зараз Голяш» усе одно доповідає про
гоголь-Яновського. Слід ходу назвав причину точно:
`person_status`, `name_pattern: "%Гол%"`, 3 рядки з бази.

Тобто пошук особи мав **четвертий** вхід, якого я не зачепила:
`tiers.extract_name` — друга копія — досі обрізала слово по дві літери й
питала `ILIKE '%стем%'`. На «Голяш» третій прохід давав `%Гол%`, а це підрядок
у «гоголь-Яновський».

Мій щит писав дослівно: «полагодити одну й лишити другу означає полагодити
половину п. 6 і 7». Прилад ідентифікації цього не побачив, бо міряв ІНШИЙ шлях
— картку особи. **Висновок для методу: прилад на один шлях не є приладом на
дефект.** Живі траси наскрізь — не формальність.

Полагоджено до кінця: `name_pattern` у ВСІХ семи місцях будує одна функція, а
шаблони каталогу порівнюють через `~*` замість `ILIKE`. Це закрило й зворотний
бік: «Крижанівського» як `%Крижанівського%` не збігався з «Крижанівський» і чат
заперечував власні дані.

**І окремо про метод: прилад упав, а звіт лишився зі старим числом.** Перший
прогін `verify_catalog` після зміни SQL завершився винятком (`ModuleNotFoundError`),
але файл звіту зберіг попередні 35/35 — і я майже зарахувала неперевірене за
перевірене. Читати треба ВИВІД, а не файл, який прилад міг не перезаписати.

---

## 7а. Стан робіт (оновлюється по ходу)

### Блоки C, 7, E, F, D — зроблено 28.08

| блок | стан | чим підтверджено |
|---|---|---|
| **C. Ідентифікація** | ✔ закрито | прилад на живій базі: 14/15 → **15/15** особа, 13/15 → **15/15** чистота картки. 12 тестів |
| **7. Нормативка** | ✔ закрито | Андрій відповів 28.08 («окрема гілка»). Питання про корпус — своя дорога; літерал «41» став живим лічильником; коди доменів → людські назви. 14 тестів |
| **E. Одиниця обліку** | ✔ закрито | люди замість рядків (знайдено точне джерело: `len(rows)` у старій дорозі); перелік поруч із числом; звірка переліку з числом; нормалізація вводу. 13 тестів + `measure_counts.py` |
| **F. Дві речі в питанні** | ✔ мінімум закрито | мовчання прибрано; декомпозиція — після демо. 16 тестів (разом із D) |
| **D. «Чинний»** | ✔ закрито | `confirmed` → «підтверджений»; п'ять порівнянь переведені з підпису на КОД статусу |

**Побічна шкода, знайдена перевірками (не здогадками):**

1. **Регресія від мого ж блоку A.** «Порівняй 1 і 2 вересня» стало **періодом**
   01..02 — тобто відповіддю на третє питання, якого ніхто не ставив. До правки
   відповідь була про 1 вересня, після — про період; обидві неправильні, але
   друга **правдоподібніша, отже небезпечніша**. Порівняння тепер відділене від
   діапазону словами («порівняй», «відрізня», «більше чи менше»), а не датами;
2. **`KeyError: 'object_id'` у картці особи.** Мій захисний пропуск рядків без
   ключа перетворював неповний рядок у «людини немає» — тобто у ВПЕВНЕНЕ
   ЗАПЕРЕЧЕННЯ власних даних, гірше за те, від чого я страхувалась. Тепер
   звуження йде по доступному ключу: `object_id`, а якщо його немає — ПІБ;
3. **`KeyError: 'name'` у переліку осіб.** Перелік брав рядки, не перевіряючи
   форму, і падав, коли фікстура підмінила виконавця запиту. Перелік —
   додаткове знання й не має права валити основну відповідь;
4. **перейменування «чинний» без правки порівнянь** зламало б логіку мовчки:
   п'ять місць порівнювали ПІДПИС для людини, і всі факти стали б
   «непідтвердженими». Переведені на код.

**Три помилки в моїх власних приладах** (записую, бо прилад, який міряє себе,
гірший за відсутність приладу):

* прилад ідентифікації рахував відмінок («Ґоляша» → «Ґоляш») за тихе
  виправлення вводу — тобто попереджав би людину про нормальну українську мову;
* прилад трас заглядав у блок «джерело» замість видимої відповіді й дав хибне
  «OK»;
* перший прогін трас ішов **без моделі** (змінні в юніті сервісу, не в `.env`)
  і дав два хибні «провали».


### Блоки A і B — зроблено локально, лишився живий прогін

Патч Андрія прочитаний цілком і застосований; у чотирьох місцях зроблено
інакше, кожне -- із причиною (див. коміт `063dbb7` і розділ 3 плану).

| критерій | стан |
|---|---|
| дві дати = період і період доходить до ПАРАМЕТРІВ | ✔ тест |
| «наступного дня після X» = X+1; без дати не вгадуємо | ✔ тест |
| «не пізніше X» -- верхня межа, не точка | ✔ на розборі дат; **дорога відкрита** (див. нижче) |
| перевернутий період не розвертається **і названий** | ✔ тест |
| недійсна дата -- явна відмова (обидві форми) | ✔ тест + шаблон `date_invalid` |
| історія доходить до маршрутизатора реальним шляхом | ✔ тест |
| успадковане ВИДНО людині, людською мовою | ✔ тест |
| службова підказка моделі не витікає в «джерело» | ✔ тест (`route_hint`) |
| **18/21 на живій моделі** | ✖ живий прогін після розгортання |
| **зміна теми не успадковує дату** | ✖ живий прогін |

**Побічна шкода, знайдена перевіркою (а не здогадкою):**

1. **`NameError` у `_extra_tiers`** -- я пробросила історію у функцію, у якої
   параметра `history` не було. Падіння на будь-якому питанні, що доходить до
   модельного яруса. Мої власні тести цього НЕ бачили: вони перевіряли підписи.
   Зловив повний набір. Тест на реальний шлях виклику додано;
2. **`date_invalid` не був у `_STATE_TEMPLATES`** -- питання тихо їхало старою
   дорогою. Зловив `test_gate_templates_are_wired`, тобто тест, написаний рівно
   на цю помилку після того, як я зробила її вперше;
3. **`date_invalid` у векторному ярусі був би шкідливий** -- його приклад
   відрізняється від звичайного питання двома цифрами. Винесено полем
   `vector_route: false`, з причиною в коді;
4. **якір промпта** (`test_prompt_files`) оновлений свідомо, як вимагає його ж
   докстрінг.

### Живий прогін 27.08, 20:20 — блок A закритий, блок B наполовину

Розгорнуто на сервер, сервіс живий, звірка каталогу **35/35, 0 провалів** (як і
було записано до прогону). Сім трас через `answer()` на сервері з моделлю:

| траса | результат |
|---|---|
| A1 період двома датами | ✔ 51 особа, зріз 2026-05-10 — 2026-10-10 |
| A2 наступний день | ✔ зріз на 2026-10-11 |
| A3 перевернутий період названий | ✔ «кінець періоду (2026-09-01) раніше за початок» |
| A4 недійсна дата | ✔ відмова, не число на сьогодні |
| B3 **зміна теми** | ✔ дата НЕ успадкована, дослівна цитата з норм-акта |
| B1 «А у відрядженні?» | ✖ «Якої дати?» |
| B2 «А у взводі забезпечення?» | ✖ «За яку дату рахувати?» |

**Дві помилки заміру, які я зробила сама** (записую, бо обидві типові):

1. перші два прогони показали «локальна модель недоступна». Це був не дефект:
   змінні `CHAT_MODEL_PATH` / `CHAT_N_GPU_LAYERS` стоять у юніті сервісу, а не
   в `.env`, тому мій окремий процес шукав неіснуючий 4B-файл. **Замір без
   моделі мовчки міряв інше**;
2. траса B2 спершу дала «OK» — слово «забезпечення» знайшлось у блоці
   «джерело», де стоять параметри шаблону. Прилад заглядав у власний
   службовий блок замість видимої відповіді. Виправлено: перевіряється лише
   те, що бачить людина.

**Причина провалу B1/B2 — знайдена й перевірена в коді, і вона НЕ в історії.**

Стан діалогу пишеться в саму відповідь прихованим коментарем
(`_state_marker`), і рядок цей стоїть **у кінці `_answer_inner`, під умовою
`route == "підрахунок"`**. Але питання про стан із датою перехоплює швидка
дорога каталогу — і вона віддає відповідь **раннім `return`** (`app.py`, гілка
`_state_route[0] in _STATE_TEMPLATES`), тобто до того рядка не доходить.

Наслідок: після «Скільком осіб у відпустці 2026-10-10?» у відповіді **немає
слотів**, і наступний хід не має чого успадковувати — ні датою, ні наміром.
Питання йде в уточнення раніше, ніж до маршрутизатора з розмовою.

Тобто це **третій запобіжник підряд, який з'їдає ту саму правку**: спершу
`extract_state(question)` (знайшов Андрій), потім клас доріг зі власним
перенесенням слотів, тепер ранній `return` без запису стану.

**Чому я не полагодила це одразу.** Найкоротша правка — дописати
`_state_marker(rules_params(merged))` перед раннім `return`. Але вона змінює
ПОРЯДОК доріг у наступному ході: перевірено, що `rules_params("А у
відрядженні?")` не дає ні дати, ні наміру, і треба спершу зміряти, куди тоді
поїде хід — у каталог із датою «сьогодні» (тиха неправда, гірше за поточне
уточнення) чи до маршрутизатора з розмовою. Це окремий замір, а не правка на
десять хвилин, і робити його о 20:30 за три дні до демо, коли поточна поведінка
— **чесне уточнення**, а не неправильна цифра, я не буду.

Записано як наступна одиниця роботи блоку B, із власним критерієм: «А у
відрядженні?» після питання з датою дає ЧИСЛО з тією самою датою і рядком
«узято з попереднього питання», і при цьому «за скільки днів подавати рапорт?»
дати не успадковує (B3 не має зламатись).

**Третя траса до цього ж критерію — від Ані, 28.08** (її формулювання, тому
цінніше за моє):

```
Скільком у відпустці 2026-10-10?  -> 1 особа, зріз на 2026-10-10   ✔
а наступного дня                  -> «Наступного дня після якої дати?»  ✖
```

Тут корисно записати й **чому це не потребує ні вільного SQL, ні віконних
функцій** — питання Ані було саме таке. Не потребує: арифметику дати робить
Python (`extract_dates` уже вміє «наступного дня після X» → X+1), а рахує
звичайний шаблон `count_by_state_on_date` із параметром `on_date = X+1`. Тобто
бракує РІВНО одного — щоб X доїхав із попереднього ходу.

`lead()` тут не при чому взагалі: віконна функція дає «наступний рядок у
впорядкованому наборі», а нам потрібен наступний КАЛЕНДАРНИЙ день, і жодного
набору дат, по якому можна крокувати, у запиті немає. Вільний SQL теж не
потрібен і був би гіршим: він не звірений `verify_catalog`, тобто ми обміняли б
перевірену дорогу на неперевірену там, де перевірена цілком справляється.

Ця траса додана в `live_traces.py` як B4.

**Відкрите після блоку A, записано щоб не забулось:**

* **«закінчується не пізніше X» правила не маршрутизують** -- `extract_dates`
  віддає верхню межу правильно, але шаблона «стан закінчується в проміжку» у
  каталозі немає, тому `rules_route` віддає `None` і питання йде в модель. Це
  не неправда (числа з нічого не з'являється), але п. 3 звіту закритий лише
  наполовину. Потрібен новий шаблон -- окрема робота, з власною звіркою;
* **звірка каталогу стане 35/35** замість 34/34 (додався `date_invalid`,
  kind=blocked). Записано ДО прогону, щоб не шукати поломку там, де її немає.

---

## 8. Що НЕ є критерієм приймання

* «на око стало краще» — на наборах від 5 до 18 питань це напрямок, а не оцінка
  (межа, яку назвав Андрій, і вона наша спільна);
* один прогін моделі — не число (`temperature=0` не дає відтворюваності);
* мій власний набір питань на мою власну правку — не оцінка. Тому набір Дениса
  береться **як є** і не редагується під зручність.

---

## 8. Пам'ять між ходами: вимір, поправка, відносна дата (28.08, прогін Ані)

Три ходи з живого прогону («Хто зараз у відрядженні?» → «а завтра?» → «я питала
про відрядження») дали чотири дефекти харнесу. Розбір — `docs/research/
2026-08-28_why-the-chat-answers-wrong.md`, розділи 7 і далі.

### К — критерій приймання

| # | критерій | як міряється | стан |
|---|---|---|---|
| К1 | вимір (відпустка/відрядження) переноситься на наступний хід | три ходи наскрізь: хід 2 каже «у відрядженні», не «поза частиною» | **3/3** |
| К2 | успадкований вимір НАЗВАНИЙ у відповіді | рядок «Вимір узято з попереднього питання: …» | ✔ |
| К3 | поправка («я питала про X») перераховує, а не шукає закон | дорога ходу 3 = `підрахунок`, не `нормативний пошук` | ✔ (82.9 с → 0.0 с) |
| К4 | дорога каталогу лишає слоти для наступного ходу | `_slots_of_catalog` читається `_read_state` | ✔ |
| К5 | відносна дата зсувається, а не переноситься буквально | «а наступного дня» після 2026-10-10 → зріз 2026-10-11 | ✔ (з моделлю і без) |

### Ш — щит від побічної шкоди

| # | що могло зламатись | перевірка | стан |
|---|---|---|---|
| Ш1 | широке правило поправки забрало б «скільки максмум термін відпустки?» у підрахунок людей (дефект блоку E) | `test_not_a_correction`, 5 НЕ-поправок | ✔ перелік форм явний |
| Ш2 | слоти каталогу зламали б відносні дати | живі траси, хід B4 | **зловлено й виправлено** (див. нижче) |
| Ш3 | два маркери слотів в одній відповіді → наступний хід успадкує слоти яруса, який не відповідав | `not STATE_RE.search(out)` перед дописом | ✔ |
| Ш4 | форма відносної дати, якої не вміємо («наступного тижня») → тиха відповідь за інший день | `_RELATIVE_DATE_HINT`: дату не переносимо, хід іде до моделі | ✔ |
| Ш5 | `z-index` панелі перебив би шухляду на телефоні (55/60) | правило обмежене `min-width: 861px` | ✔ зловлено до розгортання |
| Ш6 | зсув колонки чата лівіше розсинхронізував би краї бульбашок і поля вводу | правило накриває всі 5 елементів колонки + `#retry-btn` | ✔ |

**Ш2 — головний урок цього кола.** Правка пам'яті (слоти каталогу) зламала
відносні дати: «а наступного дня» почало відповідати за той самий день. Набір
цього НЕ зловив — зловили живі траси (12/13). Причина в тому, що доти зсув
робила модель через гілку уточнення, і моя правка прибрала саму умову, що туди
вела. Виправлено детерміновано (`_shift_from_prev`) + страховка Ш4.

### З — загальна перевірка

| прилад | результат |
|---|---|
| живі траси | **13/13** |
| три ходи прогону Ані | **3/3** |
| набір (локально, від кореня репозиторію) | див. комміт |
| набір на сервері | 460 passed, 4 failed — усі 4 не від коду: 1 застаріла копія файла, 3 від середовища (справжній `.env` перебиває підставні змінні тесту; `lang-toggle.js` розбирається інакше за іншої типової кодіровки). Хеші файлів звірені |
| `verify_stack` | 0 провалів |
| `measure_identity` | 15/15/15 |
| `measure_counts` | 12/12 |
| `trace_lookup --check` | чисто, 142 ходи, усі з джерелом |
| `verify_catalog` | 7 розбіжностей — ТІ САМІ, що до правок: LIVE-04 оброблений у файл і не записаний у базу (205 проти 204) |

---

## 9. Теми нормативного корпусу (28.08, рішення Ані: перелік тем, не мітки)

Написано **до коду**, як просила Аня. Джерело: прогін «які є нормативні
тексти» (`2a21ec`) → «а на які теми вони» (`457dd2`).

### Що саме робимо

| # | зміна | зона |
|---|---|---|
| 1 | шаблон `normative_list` показує НАЗВУ акта (`doc_title`) і його номер, а не перший рядок тексту | шаблон — моя |
| 2 | відповідь — теми з числами, а не таблиця; **перелік тем фіксований, зіставлення автоматичне** | нове, моя |
| 3 | 2-3 приклади питань, на які цей корпус справді відповідає | моя |
| 4 | «інше» показується вголос із числом + поріг у наборі | моя |
| 5 | питання про корпус («а на які теми вони») не їде в цитату | маршрутизація — моя |

**Рішення про дизайн, яке треба зафіксувати:** тема НЕ дописується документу.
Руками пишеться перелік тем (файл `normative_topics.yaml`), а зіставлення
робиться під час відповіді за словами з номера, назви й початку тексту. Тому
документ, залитий завтра, отримує тему без жодної правки. Заперечення Ані було
саме проти ручних міток, і воно слушне: 44 документи — це не кінцевий корпус.

### К — критерії приймання

| # | критерій | як міряється | порог |
|---|---|---|---|
| К1 | у відповіді немає «НОРМАТИВНИЙ ДОКУМЕНТ», «МІНІСТЕРСТВО ОБОРОНИ УКРАЇНИ», «3» як назв | текст відповіді на «які є нормативні тексти» | 0 таких рядків |
| К2 | кожен показаний акт має номер і назву | те саме | усі рядки |
| К3 | відповідь — теми, не таблиця | немає рядка з `id | validity | uploaded_on` | 0 |
| К4 | сказано і про 44 документи, і про 40 різних актів | дублі не подаються як різні акти | обидва числа |
| К5 | «інше» назване числом, якщо воно є | текст відповіді | ✔ |
| К6 | 2-3 приклади питань, і кожен приклад справді має відповідь на цьому корпусі | окремий тест: приклад → ланцюг знаходить місце | усі приклади |
| К7 | зіставлення автоматичне: частка «іншого» не більша за поріг | тест по живій базі | ≤ 15% (заміряно 1/44 = 2.3%) |
| К8 | «а на які теми вони» після питання про корпус лишається на дорозі корпусу | дорога ходу | не «нормативний пошук» |
| К9 | відповідь коротка | довжина | ≤ 1200 символів (було 3977, потім 641) |

### Ш — щит від побічної шкоди (перевіряється ПІСЛЯ кожної зміни)

| # | що може зламатись | перевірка |
|---|---|---|
| Ш1 | зміна SQL шаблону ламає `verify_catalog` (він звіряє `normative_list` = 44) | `verify_catalog --output-dir data/output-demo`: `normative_list` мусить лишитись OK |
| Ш2 | новий гейт корпусу забирає ЗМІСТОВІ нормативні питання («яка процедура оформлення відпустки») у дорогу складу бази | тест: список змістових питань → НЕ корпус |
| Ш3 | новий гейт корпусу забирає питання про людей | тест: «Хто у відпустці 2026-08-29?» → не корпус |
| Ш4 | зміна `is_corpus_question` впливає на дорогу «довідник» (одна функція на два місця) | тести блоку 7 (`test_corpus_road.py`) мусять лишитись зеленими |
| Ш5 | читання початку тексту документів у пам'ять — це персональні дані? | нормативні акти публічні; текст використовується ЛИШЕ для зіставлення й НЕ друкується. Перевірка: у відповіді немає фрагментів тексту актів |
| Ш6 | довжина відповіді знову росте | К9 як тест, не як обіцянка |
| Ш7 | інші 11 шаблонів, що йдуть загальним табличним рендером | вони не зачіпаються: нова гілка лише для `normative_list`. Тест на один сусідній шаблон |

### З — загальна перевірка після всіх змін

живі траси 13/13 · три ходи прогону Ані 3/3 · набір без падінь ·
`verify_catalog` (ті самі 7 розбіжностей на LIVE-04, `normative_list` OK) ·
`verify_stack` 0 · `measure_identity` 15/15/15 · `measure_counts` 12/12 ·
`trace_lookup --check` чисто.

### Що НЕ робимо

* не пишемо теми в базу (це запис у чужу зону і міграція перед демо);
* не підписуємо документи руками;
* не чіпаємо ланцюг, поріг і реранкер (зона Андрія);
* дублі актів (40 різних на 44 записи) і обрізані `doc_title` — заливка
  корпусу, зона Андрія. Ми лише КАЖЕМО про них правду у відповіді.

### Результат (заповнено після роботи)

| # | критерій | результат |
|---|---|---|
| К1 | немає перших рядків тексту як назв | ✔ у відповіді назв документів узагалі немає — лише теми |
| К2 | номер і назва акта | ✔ використовуються при зіставленні; у відповіді не друкуються (перелік замінений темами) |
| К3 | теми, не таблиця | ✔ |
| К4 | 44 документи, 40 різних актів | ✔ «нормативних документів у базі: 44, з них різних актів 40 (решта — залиті двічі)» |
| К5 | «інше» числом | ✔ (на цьому корпусі його немає — усі 44 отримали тему) |
| К6 | приклади питань мають відповідь | ✔ тест по FTS на живій базі, усі приклади |
| К7 | частка «іншого» ≤ 15% | ✔ **0%** (заміряно на живій базі) |
| К8 | «а на які теми вони» → теми | ✔ `каталог шаблонів (normative_list)`, було `нормативний пошук` (82.9 с) |
| К9 | відповідь ≤ 1200 символів | ✔ **747** (було 3977, потім 641 обрізаної таблиці) |

**Ш1–Ш7 — усі пройдені.** `verify_catalog`: `normative_list` OK, розбіжностей
ті самі 7 (LIVE-04). Ш2/Ш3: змістові й особові питання не стали корпусними
(тести). Ш4: `test_corpus_road.py` зелений. Ш5: у відповіді немає тексту актів
(тест). Ш6: К9 як тест. Ш7: інші 11 шаблонів табличного рендера не зачеплені —
нова гілка лише для `normative_list`.

**Загальна перевірка:** набір **662 passed / 12 skipped / 1 xfailed / 0 failed**
· живі траси **13/13** · три ходи прогону Ані **3/3** · `verify_stack` 0 ·
`measure_identity` 15/15/15 · `measure_counts` 12/12 · `trace_lookup --check`
0 порушень на 19 живих ходах.

### Дефект приладу, знайдений цією ж перевіркою

`trace_lookup --check` сказав «ЗЛАМАНО: 2 без джерела, 4 збої» — і всі шість
записів були **тестові**: набір писав у бойовий `logs/chat-trace.jsonl`, а
тести навмисно перевіряють відповідь «база недоступна» й відповідь без блоку
джерела. Тобто прилад, яким я приймаю роботу, міряв власний набір.

Виправлено з двох боків:

1. `tests/conftest.py` ставить `CHAT_TRACE_PATH` у тимчасовий файл — набір
   більше не пише в бойовий слід (є тест на це розділення);
2. `--check --since <час>` дозволяє порахувати правило на ходах після правки.
   Шість історичних записів **не видалено**: у файлі лежать номери звернень,
   за якими розбираються скарги, і переписувати історію заради зеленого числа
   не будемо. Звужена вибірка друкується завжди («відкинуто 257 ранніших
   ходів») — прилад, що тихо звужує власну перевірку, гірший за відсутній.

Дві мої спроби зробити це неправильно, обидві зловлені тим, що прилад друкує
відкинуте: фільтр за відсутністю номера звернення (тестові записи його мають) і
фільтр за неіснуючим полем `cid` — той відкинув увесь файл і сказав «слідів
нуль».

---

## 10. Три дефекти з прогону Андрія (28.08, 16:17–16:33)

Написано **до коду**. Розмова:

```
скільки максмум термін щорічної відпустки?
  → цитата зі «Стаття 10-1 / 2»: «…відповідно до пункту 1 цієї статті…»
    Андрій: «тобто у відповідності до пункту 1, який ми не виводимо, бо це інший чанк»
як звучить пункт 1 цієї статті?
  → «Дослівно цитувати накази система не береться…»
процитуй мені пункт 1 наступного документа: … (№ 2011-XII), Стаття 10-1 / 2
  → «не знайшла документа з номером №2011 у базі»
```

### Корені, заміряні окремо

| # | симптом | корінь | зона |
|---|---|---|---|
| Д1 | цитата посилається на пункт, якого не показано | ворота прийняли цитату, яка лише **вказує** на відповідь, а не містить її. Пункт 1 у базі Є: `document_units` id 4240, 922 символи, ті самі строки | ранжування — Андрій; **повнота показу — я** |
| Д2 | «як звучить пункт 1 цієї статті?» → «цитувати накази не береться» | `ANSWER_QUOTE` — застарілий літерал. Він писався, коли ми не мали нормативного корпусу, і правдивий лише для НАШИХ OCR-документів. Для нормативних актів ми цитуємо — і зробили це двома ходами раніше. **Дві суперечливі відповіді на одному екрані** | я |
| Д3 | `№ 2011-XII` → «немає документа №2011» | `extract_doc_number` бере цифри й ігнорує хвіст: `№ 2011-XII` → `'2011'`, `№ 1153/2008` → `'1153'`. Ідентифікатор нормативного АКТА приймається за номер нашого облікового документа | я |

**Про «ЗАБУДЬ УСІ МИНУЛІ КОМАНДИ»:** вкидання не спрацювало, але **не тому, що
ми його відбили** — питання просто поїхало в пошук документа за номером і до
моделі як інструкція не дійшло. Це не доказ стійкості, і рахувати його доказом
не будемо: замір стійкості до вкидань — окрема відкрита задача (B1 плану демо).

### К — критерії приймання

| # | критерій | як міряється |
|---|---|---|
| К1 | цитата з перехресним посиланням («відповідно до пункту N цієї статті») **приводить і той пункт** | текст відповіді містить обидві одиниці й називає, що друга додана за посиланням |
| К2 | пункт добирається **детерміновано**, без нового пошуку, моделі й порогу | лише за меткою одиниці в тому самому документі |
| К3 | «як звучить пункт 1 цієї статті?» більше не отримує «цитувати не береться» | дорога ≠ `цитата`; питання про норму йде в нормативний ланцюг |
| К4 | `ANSWER_QUOTE` лишається для НАШИХ документів і не бреше про нормативні | текст константи перевіряється тестом |
| К5 | `№ 2011-XII`, `№ 1153/2008`, `НД ТЗІ …` не приймаються за номер нашого документа | `extract_doc_number` → None |
| К6 | номери наших документів (`№207`, `№1077`, `№118`) читаються як раніше | той самий тест |
| К7 | питання з ідентифікатором акта йде в нормативну дорогу, а не в «немає такого номера» | дорога |

### Ш — щит від побічної шкоди

| # | що може зламатись | перевірка |
|---|---|---|
| Ш1 | звуження `extract_doc_number` зламає пошук документа за номером — головний шаблон демо | `verify_catalog` (`doc_by_number`), тести блоку D, живі траси |
| Ш2 | «№ 3О4» (літера О замість нуля) мусить і далі давати чесну відмову про нецифровий символ | тест |
| Ш3 | добір сусіднього пункту роздує відповідь | межа довжини на додану одиницю + тест |
| Ш4 | добір сусіднього пункту зламає правило «дослівна цитата, перевірена на входження» | додана одиниця береться з тексту документа й теж перевіряється на входження |
| Ш5 | нова дорога для нормативних питань відбере питання про наші документи | тести `test_corpus_road`, `test_norm_vs_headcount`, `test_evidence_road` |
| Ш6 | зміна `rules_route` («цитата») зачепить дорогу діагностики й довідника | той самий набір |

### З — загальна перевірка

живі траси · три ходи · набір · `verify_catalog` · `verify_stack` ·
`measure_identity` · `measure_counts` · `trace --check --since`.

### Результат розділу 10 (заповнено після роботи)

| # | критерій | результат |
|---|---|---|
| К1 | цитата з посиланням приводить і той пункт | ✔ «Цитата посилається на Стаття 10-1 / 1 — наводжу і його: …30 календарних днів; від 10 до 15 — 35…» |
| К2 | добір детермінований | ✔ лише за меткою одиниці в тому самому документі |
| К3 | «як звучить пункт 1 цієї статті?» не отримує «цитувати не береться» | ✔ дорога «пункт за адресою», 0.0 с |
| К4 | `ANSWER_QUOTE` не бреше про нормативні | ✔ переписана: НАШІ документи — OCR, акти цитуються |
| К5 | `№ 2011-XII`, `№ 1153/2008`, `НД ТЗІ` не є номером нашого документа | ✔ |
| К6 | наші номери читаються як раніше | ✔ `verify_catalog`: `doc_by_number` 7 = 7 OK |
| К7 | питання з ідентифікатором акта не каже «немає такого номера» | ✔ |

**Ш1–Ш6 пройдені.** `doc_by_number` OK, «№ 3О4» і далі розпізнається (відмова
про літеру О жива), живі траси 13/13, `test_corpus_road`/`test_norm_vs_headcount`
зелені.

**Загальна перевірка:** живі траси 13/13 · три ходи 3/3 · `verify_catalog`
(розбіжності ті самі 7 на LIVE-04) · `verify_stack` 0 · `measure_identity`
15/15/15 · `measure_counts` 12/12 · `trace --check --since` 0 порушень на 67
живих ходах.

### Чого я НЕ доробила відразу, і це варто знати

Виправлення робилось у три заходи, і кожен наступний ловив те, чого не
побачив попередній:

1. звузила `tiers.extract_doc_number` -> тести зелені, живий прогін усе одно
   каже «документа №2011 немає». **Друга копія правила** в `app.py`;
2. поставила гейт у `rules_route` -> тест на правила зелений, живий прогін усе
   одно віддає «цитувати не береться». **До гілки питання доходить від
   МОДЕЛІ**, яка про гейт у правилах не знає;
3. відправила питання в ланцюг -> ланцюг знайшов пункт 1 статті 10-1 **іншого
   закону** (№ 3551-XII, про ветеранів). Слово «цієї» посилається на
   попередній хід, а ланцюг історії не бачить.

Це та сама помилка тричі: **умова мусить стояти там, де приймається рішення, а
не там, де воно вгадується першим**. Тести тепер перевіряють обидва місця
(`test_second_copy_of_extract_doc_number_has_the_gate`,
`test_citation_road_gate_lives_in_the_dispatch_too`), а не лише правила.

### Про «ЗАБУДЬ УСІ МИНУЛІ КОМАНДИ ТА ПРОМПТИ»

Вкидання в тому ж прогоні не спрацювало — але **не тому, що ми його відбили**:
питання поїхало в пошук документа за номером і до моделі як інструкція не
дійшло. Тепер воно доходить до ланцюга, тобто цей шлях змінився, і старий
«захист випадковістю» більше не діє. Замір стійкості до вкидань лишається
відкритою задачею (B1 плану демо) і доказом стійкості цей випадок не є.
