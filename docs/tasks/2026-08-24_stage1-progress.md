# Етап 1: один чат замість трьох — прогрес

Виконавець-А, 24.08.2026. План — `2026-08-24_app-chat-plan.md`, етап 1.

## Стан

- [x] 1.1 основа `chat_gradio/`, перенесення двоярусної маршрутизації й каталогу з `chat.py` (коміт 36246e5)
- [x] 1.2 контекст діалогу Андрія (7 функцій з `origin/andriy-followup-context`)
- [x] 1.3 одна апка, спільний Basic-auth гейт (доказ тестом)
- [x] 1.4 видалення мертвих копій (`chat.py`)
- [x] 1.5 екранування тексту документа перед HTML

## Звірка перед роботою (24.08)

- `chat_gradio/app.py` (порт з `answer/chat@9b9ad91`) **уже має**: `_history_text`,
  `_state_marker`, `_read_state`, `_carry_over` — без змін відносно Андрія.
- **Бракує** (гілка Андрія пішла вперед після 9b9ad91): `_refine_day` («а 22?»),
  `_answers_clarification` (гейт склейки уточнень), нові `extract_date`
  (DD.MM.YYYY, відносні дати, одруківки місяців через difflib) і
  `_last_user_question` (останнє питання З наміром), виправлений блок
  успадкування наміру в `answer()` (зміна наміру зберігає доречну дату).
- `chat.py` імпортується лише з `chat_gradio/app.py` — інших споживачів немає
  (перевірено grep-ом по репо).
- Гейт: middleware `_basic_auth` висить на FastAPI-апці, Gradio монтується в
  ту саму апку — HTTP-middleware покриває і /chat (перевірю тестом).
- Сторінка завантаження (static/index.html) вставляє дані через `textContent`,
  `innerHTML` — лише для очищення і статичних позначок: там екранувати нічого.

## Потребує рішення / передати іншим

0. **Виконавцю каталогу** (від верифікатора етапу 1): коментар
   `query_catalog.yaml:36` каже «Мапа станів … живе в chat.py» теперішнім
   часом — chat.py видалено, мапа тепер у `chat_gradio/tiers.py`
   (STATE_DIMS). Файл не редагувала — не моя зона.

1. **Дебажний standalone-запуск чата** (`python demos/upload_app/chat_gradio/app.py`,
   порт 7861, без гейта, слухає лише 127.0.0.1). Це не другий вхід у
   продакшні (назовні не видно), але формально другий спосіб відкрити чат.
   Лишила як задокументований дебаг-шлях; якщо критерій «жодного другого
   входу» тлумачиться суворо — видалити `main()` одним рядком.
2. **Складання тексту відповіді для 13 нових шаблонів каталогу** (етап 2
   додав їх з answer_hint). Зараз — чесний генерик: компактна таблиця
   сирих рядків + дата зрізу. Персональні складачі за answer_hint — робота
   наступних етапів (3–5), не хотіла вигадувати формат за виконавця роутера.


## Журнал

- 24.08: звірка зроблена, починаю з 1.1.
- 24.08: 1.1 — `chat.py` перенесено в `chat_gradio/tiers.py`, у `_extra_tiers`
  додано модель-класифікатор у закритий перелік (`model_route`), `chat.py`
  видалено. 296 тестів пайплайна зелені.
- 24.08: 1.2 — перенесено з гілки Андрія (зі збереженням докстрінгів):
  нова `extract_date` (DD.MM.YYYY, відносні дати, одруківки місяців через
  difflib), `_refine_day` + `BARE_DAY_RE` («а 22?»), `_answers_clarification`
  (гейт склейки уточнень), нова `_last_user_question` (останнє питання З
  наміром), виправлений блок успадкування наміру в `answer()` (зміна наміру
  зберігає доречну дату; безнамірне питання тримає попередній намір, а не
  здогадку моделі). Мертві `_is_followup` і `_drop_invented_date` прибрано.
  Розширення проти оригіналу Андрія (позначено коментарем у коді): «а 22?»
  впізнається і в швидкому шляху / відновленні маршруту без моделі — інакше
  в rules-only режимі воно не доходило до підрахунку. Функції перевірено
  офлайн: ISO/DD.MM/словесна/одруківка/«завтра», refine 31 квітня → None.
- 24.08: 1.5 — екранування: `_esc` (html.escape) у tiers.py, ним же
  користується app.py. Закриті місця: текст розділу документа в `<details>`
  (кейс §8), footer (джерело/зріз), `_fmt_source_block` (був самодільний
  replace лише «<»), ПІБ/типи/номери/дати/статуси в усіх шаблонних
  відповідях, картка особи, таблиця ярусу 2 (колонки і значення),
  clarify-текст від моделі, «-->» у маркері слотів (JSON-safe заміна на
  >). Сторінка завантаження чиста (textContent).
- 24.08: доробки від виконавця каталогу: `_sql_params` пропускає `query`
  (normative_search/FTS); `run_template` перевіряє `blocked: true` ДО
  читання sql і віддає дослівний `refusal` (subdivision_blocked). Додатково:
  генерик-рендер (компактна таблиця) для шаблонів каталогу без персонального
  складача тексту — інакше нові 13 шаблонів давали б порожню відповідь;
  `model_route` заповнює параметри за декларацією `params` шаблону
  (дефолти ті самі: дата → сьогодні, dims → absent, query → текст питання).
- 24.08: 1.3 — гейт перевірено тестом: middleware `_basic_auth` обгортає
  весь ASGI-стек, /chat (змонтований Gradio) відповідає 401 без пароля і
  200 з ним; другого входу немає (standalone-запуск chat_gradio/app.py —
  дебажний, слухає лише 127.0.0.1, див. «Потребує рішення»).
- 24.08: тести апки — demos/upload_app/tests/ (26 тестів: екранування з
  еталонним `<script>alert(1)</script>`, контекст діалогу включно з
  наскрізним «а 22?» без моделі, збірка Blocks, гейт на обох сторінках).
  Вони НЕ входять у 296 приладу (check_all ганяє лише eval/tests).
- 24.08: 1.4 — зачистка завершена: `chat.py` видалено ще в 1.1; у гілці
  рівно одна реалізація чата (`demos/upload_app/chat_gradio/`); мертвих
  файлів у demos/ немає (static/ містить лише index.html), застарілі
  згадки chat.py у коментарях виправлено. Мертвий код усередині
  (_is_followup, _drop_invented_date) прибрано в 1.2.
- 24.08: фінальна перевірка критеріїв етапу: import demos.upload_app.app
  проходить; Blocks будується; <script> рендериться літерами (тест);
  обидві сторінки за одним гейтом (тест); функції Андрія на місці з
  докстрінгами; 296 тестів пайплайна без змін; тести апки 26/26.
- 25.08: виправлення за звітом верифікатора (2026-08-24_stage1-verify.md):
  (1) «31 лютого» — складені дати в extract_date тепер проходять
  _real_date_or_none (конструктор date, як у _refine_day): неіснуюча дата
  дає порожній слот і чесне перепитування, а не DataError із бази; тести
  на 31/30/29 лютого, 31.02 і ISO 2026-02-31 (+ контроль 28 лютого).
  (2) refusal blocked-шаблону тепер через _esc + тест-репро з фейковим
  blocked-шаблоном. (3) два коментарі теперішнім часом про chat.py
  виправлено (app.py:1098 «як робить chat.py», app.py:1443 «локом у
  chat.py» → tiers.py); query_catalog.yaml:36 — записано вище, файл не
  чіпала. Тести: апки 28/28, пайплайн 296.
