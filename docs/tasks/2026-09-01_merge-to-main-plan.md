# Мердж у main: порядок і наперед розібрані конфлікти

01.09.2026. Рішення Ані: **спершу мерджиться Андрій, ми акуратно після нього.**

## Чому такий порядок

Спільний предок нашої гілки й `andriy-db` — **14.08**. Після нього ми обидва
додали ОДНІ Й ТІ САМІ файли незалежно, тому git бачить `add/add`: він не знає,
що це те саме, бо спорідненості в історії немає.

Я це ще й підсилила: 30.08 скопіювала його код (гейт перетину в лоадері,
`renderConflict` в index.html) **руками з його комміта**, а не мерджем. Ті самі
рядки тепер додані двічі з різних сторін.

Якби ми мерджились першими, конфлікти дісталися б ЙОМУ — як «моя гілка не
мерджиться в main». Тому він перший: його гілка менша, і його код опиниться в
main зі своєю історією.

## Порядок

1. **Андрій:** `andriy-db` → `main`.
2. **Ми:** `git fetch`, потім `origin/main` → `anya-pipeline` **локально**,
   розібрати конфлікти (перелік нижче), прогнати тести.
3. **Ми:** `anya-pipeline` → `main`. Після кроку 2 конфліктів там уже не буде.

Крок 2 робиться саме в нашій гілці, а не в main: якщо щось піде не так,
ламається наша копія, а main лишається цілим.

## Дев'ять конфліктів і рішення по кожному

Перевірено `git merge-tree` (нічого не змінює) 01.09.

| Файл | Що робити | Чому |
|---|---|---|
| `.env.example` | **об'єднати** (+4 −5) | обидва додавали свої змінні; втратити чиюсь = зламати чужий запуск |
| `.gitignore` | **об'єднати** | у злитому з main обидва набори вже вижили: і виняток `data/generator/templates/*.docx`, і наші рядки про файли з ключем |
| `db/scripts/build_chunk_index.py` | **його версія** | його зона (+8 −2) |
| `db/scripts/extract_document_identity.py` | **його версія** | його зона (+64) |
| `db/scripts/search_hybrid.py` | **його версія** | його зона (+68 −16) |
| `db/scripts/search_units_test.py` | **його версія** | його зона (+206 −21); наш `normative_chain` імпортує з нього `SCHEMA`, тому після мерджу прогнати нормативну дорогу |
| `demos/upload_app/app.py` | **наша версія** | у його версії немає **жодного** рядка, якого немає в нашій: його `except OverlapConflict` і `/supersede` я вклала 30.08 дослівно з його комміта, а наші `/jury` і `/static/qr-jury.png` є лише в нас |
| `demos/upload_app/static/stats.html` | **наша версія** | різниця лише в КОМЕНТАРЯХ (перевірено без них: змістовних розбіжностей нуль). Наша вже містить його три прибирання від 29.08 плюс повернений рядок про відкинуті метрики |
| `demos/upload_app/tests/test_partner_metrics.py` | **наша версія** | те саме: його зміни вже враховані, наша додатково знає про `same_as_live` (11 записів = 6 показано + 4 згорнуто + 1 дубль перетинів) |

Правило, за яким це вирішено: **у чужій зоні — його версія, у нашій — наша, у
спільних файлах — та, що вже містить обидві роботи.**

## Що перевірено щодо самого main

| Питання | Відповідь |
|---|---|
| конфлікти з `origin/main` | **0**, мердж чистий |
| main торкався нашого коду | **жодного файлу**: лише `data/generator/` і `docs/` |
| main щось видаляє | ні |
| наша гілка попереду | 415 коммітів; main попереду нас на 9 |
| документи/картинки поза `data/eval` | один — `docs/demo/qr-demo.png`. Це QR посилання **без ключа** (пароль у ранбуку). Рішення Ані 01.09: лишаємо |

## Після мерджу — обов'язково

1. прогін суїти `demos/upload_app/tests/` і `eval/tests/` на злитому дереві;
2. окремо нормативна дорога: вона імпортує `SCHEMA` з `db/scripts/search_units_test.py`, а цей файл беремо його версією;
3. ~~`verify_stack.py` і `verify_catalog.py`~~ — **більше нічим**:
   віртуалку вимкнули 01.09 посеред звірки (розбір —
   `2026-09-01_server-shutdown-what-was-lost.md`). Лишаються локальні тести:
   вони не бачать ні бази, ні моделі, і це треба сказати вголос ПЕРЕД
   мерджем, а не після.

## Що мердж справді зламав — нормативна дорога (01.09, після мерджу)

Пункт 2 «після мерджу» спрацював: попередження Андрія було по адресу, але
поломка виявилась гіршою, ніж очікували. У його версії
`db/scripts/search_units_test.py` **схеми немає взагалі** — таблиці задаються
повними іменами з оточення (`UNITS_TABLE`, `GROUPS_TABLE`, `SYNONYMS_TABLE`),
а не збираються як `{SCHEMA}.таблиця`. Наш `normative_chain.py` згадував
`su.SCHEMA` у трьох місцях, і кожне ламалось інакше:

| Де | Що сталося б |
|---|---|
| перетин питання й цитати (`_overlap`) | `su.SCHEMA` — атрибута немає → **AttributeError на кожній нормативній відповіді**. Це падіння, і це добре: видно |
| добір сусіднього пункту | `getattr(su, "SCHEMA", "public")` — тихо підставляє `public`, навіть якщо таблиці лежать інде |
| перевірка доступу при старті | дивилась у схему, а не в таблицю, з якої читають його функції: «доступ є» могло стосуватись не тієї таблиці |
| налаштування його модуля | `su.SCHEMA = ...` присвоювало атрибут, якого ніхто не читає — тобто наше «перевизначення схеми» стало пустим місцем |

**Як полагоджено.** Назва таблиці тепер **одна на модуль**: `UNITS_TABLE` і
`GROUPS_FULL` збираються з `CHAT_UNITS_SCHEMA` один раз, і всі чотири місця
беруть їх. Розійтися між собою вони більше не можуть. Його модуль
налаштовується через ті самі назви (`su.UNITS`, `su.GROUPS`); стара версія
модуля теж підтримана — гілка `su.SCHEMA` лишилась під `hasattr(su, "UNITS")`.
Його теку не правили: папка — одна зона відповідальності.

**Чим це перевірено, а чим ні.** Перевірено: обидва модулі імпортуються, усі
сім функцій, які ми кличемо, на місці, імена таблиць з двох сторін збігаються
(`document_units` / `document_groups`), локальна суїта. **Не перевірено
наскрізь** — живого стенду немає, тому «пошук по нормативці справді знаходить»
цим не доведено. Перший запуск на новому сервері мусить починатися саме з
нормативного питання.

**Лишається відкритим.** Синоніми в його модулі за замовчуванням беруться з
`andriy_test.synonyms` — експериментальної схеми. Якщо роль readonly її не
бачить, пошук не падає: функція робить відкат і пише скаргу **в лог**, а
відповідь просто виходить без розширення скороченнями. Тобто деградація тиха
для того, хто дивиться на екран. Ім'я таблиці — рішення Андрія, не наше:
питаємо його, а не вгадуємо.

## Чого цей план не робить

Не чіпає гілок Дениса й Колі. Після нашого мерджу їхні гілки треба буде злити
з великим main — це робота, не конфлікт. Рішення Ані 01.09: у них немає нічого
надто важливого, тому окремо не готуємось.
