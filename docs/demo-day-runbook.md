# Ранбук демо-дня

_25.08.2026, етап 7.6. Одна сторінка: що робимо в якому порядку, хто що
натискає, і що робимо, коли щось не запуститься. Дата демо в розрахунках —
**28.08.2026**; якщо дата зміниться, перерахувати відповіді
(`data/eval/samples/demo-story/demo-questions.md` — усі періоди зсуваються
разом із датою)._

## Зранку, до демо — 4 дії по черзі

> **УВАГА, змінено 25.08.2026: перезавантажувати сервер НЕ ТРЕБА.**
> Установка CUDA-toolkit знесла GRID-драйвер vGPU (розбір і що потрібно від
> Антона — `docs/tasks/2026-08-25_gpu-driver-incident.md`). Модуль ядра
> лишився завантаженим, обчислення працюють, але після перезавантаження карта
> не підніметься, доки Антон не перевстановить драйвер. Отже: **не
> перезавантажувати, доки в переліку пакетів не з'явиться драйвер знову.**
> Демо від карти не залежить — без неї воно просто повільніше.

1. **Перевірити, що апка жива:** `systemctl is-active ai-secretary-app` і
   відкрити `http://185.9.41.1:7302` із **зовнішнього** браузера (не з
   сервера). Апка — systemd-сервіс, тобто піднімається сама; якщо ні —
   `sudo systemctl restart ai-secretary-app`.
2. **Перевірити, що карта ще рахує** (`nvidia-smi` видалений інцидентом, тому
   так):
   `cd ~/anya/ai-secretary && .venv/bin/python -c "import torch;print(torch.cuda.is_available())"`
   `False` — не панікувати: пайплайн сам відкатиться на процесор
   (запобіжник у коді + тести), просто фото буде ~50 с, а питання в чаті —
   десятки секунд замість 0.2 с.
3. **Задати одне питання з тест-сету** і порівняти цифру з
   `demo-questions.md`. Якщо цифра та — живі всі три: база, шаблони, модель.
4. **Не завантажувати нічого «на пробу» з демо-файлів**: дедуплікація за
   хешем не переобробить той самий файл двічі, і на показі це виглядатиме як
   «нічого не сталося».

## Порядок показу

| # | що показуємо | хто | скільки |
|---|---|---|---|
| 1 | проблема на словах: стос документів і питання «скільком зараз у відпустці» | — | 1 хв |
| 2 | **живе завантаження**: `LIVE-01.docx` (документ, якого система не бачила) → видно витягнуті поля | Аня | 2 хв |
| 3 | те саме з **фото** `LIVE-03.jpg` — саме воно вражає, бо це знімок телефоном | Аня | 1 хв |
| 4 | питання в чат: **«Скільком зараз у відпустці?»** → цифра + джерело + згадка про чернетки | Аня | 1 хв |
| 5 | питання **«Чи чинне посвідчення № 214?»** — показує зв'язок документ→документ, який звичайний запит не бачить | Аня | 1 хв |
| 6 | питання **поза доменом** («яка завтра погода») → чесна відмова. Це не баг, це продукт | Аня | 30 с |
| 7 | сторінка «Статистика»: скільки документів, скільки фактів, скільки чекає підтвердження | Аня | 1 хв |

Порядок не випадковий: 2-3 доводять витяг, 4-5 доводять відповіді, 6
доводить межі, 7 доводить, що цифри є.

## Правила під час показу

- **Не запускати завантаження і питання одночасно.** Модель одна, стоїть лок:
  друге чекатиме на перше. Виглядатиме як «зависло».
- **Одне питання за раз, дочекатись відповіді.** З моделлю це до 60 с; поки
  чекаємо — розповідаємо, чому відповідь довга (усе локально, жоден документ
  не покидає сервер).
- **Показувати індикатор**, а не порожній екран: якщо його не видно —
  сказати вголос «зараз рахує», а не молчати.
- **Питання беремо з `demo-questions.md`.** Не імпровізувати цифрами: там
  відповіді пораховані з еталонів і їх можна звірити.

## Якщо щось упало

| симптом | що робити |
|---|---|
| сторінка не відкривається | перезапустити апку; поки піднімається — розповідати п. 1 сценарію |
| чат каже «база недоступна» | перезапустити Postgres; цифри в базі не втрачаються |
| відповідь довша за хвилину | не тиснути повторно (стане в чергу) — дочекатись |
| модель не завантажилась | чат далі відповідає на шаблонні питання без моделі (яруси правил і векторів) — беремо питання 4 і 5, вони шаблонні |
| впало все | **план Б: записаний прогін + скріншоти.** Підготувати ЗАЗДАЛЕГІДЬ, не в день демо |

## Що НЕ показуємо і чому

- **реальні документи** — жодного; усе показане синтетика, згенерована нами.
  Це не обережність на демо, це умова існування продукту;
- **питання про підрозділи** («хто відсутній у 2 роті») — штатки в базі поки
  немає, чат чесно відмовляє. Якщо спитають із залу — це відповідь, а не
  виправдання;
- **ярус вільного SQL** — виключений свідомо (рішення 25.08): модель може
  скласти безпечний запит не на те питання, а впевнена неправильна цифра
  гірша за відмову.

## Що приготувати заздалегідь (не в день демо)

- [ ] записаний прогін демо (екран) + 5-6 скріншотів на випадок повного збою;
- [ ] `LIVE-01.docx`, `LIVE-02.pdf`, `LIVE-03.jpg` перевірені **на сервері**,
      а не лише локально;
- [ ] цифри з `demo-questions.md` звірені з живою базою в день Х;
- [ ] сценарій прогнаний вголос один раз від початку до кінця — з таймером.
