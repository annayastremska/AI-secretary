# Виправлення в пайплайні за рев'ю 22.08.2026

Вхід: `verdicts.md` (адверсарно верифіковані знахідки). Ділянка — `pipeline/**`
і `docs/known-weak-spots.md`. `eval/**` виправляє інший виконавець паралельно.

## Як міряється регресія

Паралельний виконавець змінює `eval/evaluate.py` ПО ХОДУ (додає перевірки за
A-01/A-09), тому «224/224» на робочому дереві вже не відтворюється: прилад
станом на цю сесію дає **240 перевірок**. Щоб цифра лишалась порівнюваною,
базова лінія міряється приладом З HEAD `2e344ba` (копія `evaluate.py` у
scratchpad, `PYTHONPATH=<scratchpad> python -m eval_head.evaluate`), а не
робочою копією:

```
python -m pytest eval/tests -q                                        -> 226 passed
eval_head.evaluate --no-llm --input .../leave/synthetic-2026-05/docx  -> 224/224 (100.0%)
eval_head.evaluate --no-llm --input .../deployment/.../docx           -> 183/183 (100.0%)
нормативний корпус (42 файли, process_file)                           -> 41 confirmed / 1 unresolved
```

Робочий прилад (з правками паралельного виконавця) на тих самих даних дає
240/240 (100.0%) і 14/14 шаблонів — тобто відсоток тримається в обох мірках.

## Зроблено

### C-02 — `supersession_note` давав `supersedes` зі згадки в тексті [ЗАКРИТО]

Файли: `pipeline/schemas/leave_ticket.yaml`,
`pipeline/schemas/deployment_certificate.yaml`.

**Репро ДО** (LEAVE-001 + один дописаний абзац, повний ланцюг через
`process_file`):

```
'ВПД анульовано, видано нові.'                       confirmed=True  supersedes evidence='анульовано'
'Порядок анульовання квитків визначено інструкцією.' confirmed=True  supersedes evidence='анульовання'
'Відпустка перервана достроково не була.'            confirmed=True  supersedes evidence='перервана'
```

**Фікс:** позначка мусить стояти в ДУЖКАХ і бути окремим словом
(`\((?P<value>[^()]{0,120}?\b(?:…)\b[^()]{0,120}?)\)`). Дужки — не
косметика: на всіх трьох парах еталона позначка надрукована саме в дужках, а
згадка в суцільному тексті — ні. Значенням стає весь вміст дужок, тобто
рев'юер бачить позначку так, як вона надрукована.

**Репро ПІСЛЯ:** усі три штучні випадки — `document_links = []`; справжні пари
лишились:

```
LEAVE-014 supersession_note='перервана, відкликаний з відпустки'         № None
LEAVE-016 supersedes_document_number='157' + note='виданий замість анульованого квитка № 157'
TRIP-014  supersedes_document_number='254' + note='переоформлено замість посвідчення № 254'
```

Лишається відкритим (звужено, не закрито): згадка В ДУЖКАХ усередині
суцільного тексту правило пройде.

### C-03 — реквізити чужого наказу як підтверджені факти [ЗАКРИТО]

Файли: `pipeline/extraction/extract.py` (`extract_field_regex`,
`AMBIGUOUS_MATCH_METHOD`), `pipeline/build_record.py` (raw_text для рев'юера).

**Репро ДО** (LEAVE-001 + «Відповідно до наказу командира № 777/К від
01.01.2020 р.»): `document_number = 777/К` (правильно 102),
`document_date = 2020-01-01` (правильно 2026-05-09), обидва
`method=matched, resolved=True, confidence=0.9`, і обидва доїжджають у
`facts` ОКРЕМИМИ рядками з `confirmed=true`.

**Фікс:** `pattern.search` → `finditer`; більше одного РІЗНОГО збігу одного
варіанта → `None, "ambiguous_multiple_matches:<кандидати>"`. Однакові збіги
неоднозначністю не є. Правило загальне для всіх regex-полів, а не для двох:
«бери перший збіг у документі» ніде не було обґрунтоване, воно дісталось від
`search()`.

**Репро ПІСЛЯ:**

```
document_number  method='ambiguous_multiple_matches:777/К, 102'      resolved=False  raw_text='777/К, 102'
document_date    method='ambiguous_multiple_matches:01 01 2020, 09 05 2026'  resolved=False
facts із source_field document_number/document_date: []
базовий LEAVE-001: номер=102, дата=2026-05-09, confirmed  (без змін)
```

**Регресія:** 226 passed; 224/224; 183/183; нормативний корпус 41/1.

### C-06 — хибний ДОКАЗ порожнечі слота [ЗАКРИТО]

Файли: `pipeline/extraction/blank_form.py` (`printed_skeleton_repeats`,
`MIN_REPEATED_CUTTERS`), `pipeline/extraction/extract.py`
(`empty_proof_allowed` на всіх чотирьох місцях, де оголошується
`confirmed_empty_slot`).

**Репро ДО** (LEAVE-001 + дописаний порожній примірник полів того самого
бланка): `actual_return_date`, значення якого В ДОКУМЕНТІ Є (2026-05-23),
отримує `method='confirmed_empty_slot:empty_pattern'`, тобто «ДОВЕДЕНО, що на
бланку цього немає».

**Чому мінімальна версія з пропозиції не працює:** «скелет порожнечі
збігається більше одного разу» цей випадок НЕ ловить — заміряно, збіг рівно
один (`порожній бланк: 1, LEAVE-001: 0, LEAVE-001 + примірник: 1` на кожному з
чотирьох полів зі скелетом). Дискримінатор інший і без порогу «на око»:
скільки РІЗАКІВ бланка стоять у документі частіше, ніж у самому бланку.
Замір на всіх 34 docx/pdf обох корпусів (включно з порожніми бланками й чужою
редакцією) — надлишок **рівно 0** на кожному; штучний документ із другим
примірником — **25 з 27**. Порівнюється саме з бланком, бо бланк сам друкує
частину рядків двічі (5 із 27 у квитку, 8 із 27 у посвідченні).

**Фікс:** доказ порожнечі (обидва рівні, П5-А і П5-Б) не оголошується, якщо
скелет бланка стоїть у документі двічі АБО якщо форма не впізнана
(`form_recognized=False` — третя частина пропозиції: досі це тримала емпірика
одного файлу, не умова в коді).

**Репро ПІСЛЯ:** те саме поле — `method='ambiguous_label'`, `resolved=False`,
тобто звичайна прогалина з фолбеком, а не доказ. Контроль: LEAVE-011
(справді порожній) зберіг усі 8 `confirmed_empty_slot`; чужа редакція — 0, як
і було.

**Регресія:** 251 passed / 1 xfailed (тести паралельного виконавця вже в
дереві); leave docx 224/224 і pdf 224/224; deployment docx 183/183 і pdf
183/183.

## Блок «мова схем і валідатор» (A-14, A-04, C-07, A-11, A-16, A-07 частково)

### A-14 — не-docx `blank_template` валив увесь батч [ЗАКРИТО]

**Репро ДО:** `validate_schema` зі `blank_template`, що вказує на .pdf або
.jpg → `PackageNotFoundError` назовні (перевірено на обох розширеннях).
**Фікс:** `_read_lines` ловить виняток і віддає порожній перелік (для решти
модуля це те саме, що відсутній файл), а валідатор дає ОКРЕМУ помилку «не
.docx» словами. **ПІСЛЯ:** обидва випадки — `error`, батч не падає.

### A-04 — `normalization` на полях ПІБ мовчки не діяв [ЗАКРИТО]

`extraction: rank_and_name_tokenized` іде власною гілкою `build_record`
(морфологію вибирає `part:`), тож `normalization: nominative_case` там не
читався ніколи. **Фікс:** новий перелік
`EXTRACTION_DISPATCHED_BEFORE_NORMALIZATION` (`rank_and_name_tokenized`,
`derived_from`) → оголошення `normalization` на такому полі стало `error`
валідатора; шість мертвих оголошень прибрано з двох схем (по три на схему) з
коментарем, чому вони були мертві. **ПІСЛЯ:** валідатор ловить оголошення,
чинні схеми — 0 помилок, цифри не змінились.

### C-07 — `consistency` для number-полів [ЗАКРИТО як вимога валідатора]

Заземлення числа перевіряє лише «чи є це число в документі», тому день,
місяць і хвіст номера проходять однаково з правильним значенням; другий шар
(`consistency`) був ЗБІГОМ КОНФІГУРАЦІЇ. **Фікс:** `type: number` без
`consistency` — `error`, якщо поле може прийти від моделі
(`llm_fallback` не `false` і `extraction` не `derived_from`). Автору схеми
лишається реальний вибір: правило узгодженості або `llm_fallback: false`
(відмова краща за вигадку). **ПІСЛЯ:** чинні схеми — 0 помилок; копія схеми
без `consistency` на `duration_days` — error; та сама копія з
`llm_fallback: false` — чисто.

### A-11 + A-16 — неконтрольоване значення факту й мертвий довідник [ЗАКРИТО]

- A-11: `db_target: fact_value` без `category:` тепер дає попередження
  валідатора з наслідком («підрахунок за видом стане порівнянням рядків»), а
  обидві схеми оголосили `value_free_text: true` — тобто вільний текст
  сказаний вголос, а не вгадується з відсутності ключа.
- A-16: `leave_type.yaml` НЕ видалено (коди знадобляться, якщо вид відпустки
  виділять в окреме категоріальне поле) — натомість
  `identification.unused_dictionaries` + попередження прогону: «довідник
  завантажено, але на нього не посилається жодне поле жодної схеми». Дзеркало
  `missing_dictionaries`: «лишається на майбутнє» і «працює зараз» тепер
  виглядають по-різному.

### A-07 — `validity_model` тепер їде разом із фактом [НАША ЧАСТИНА ЗАКРИТА]

`run.annotate_validity_model` дописує кожному факту `validity_model` з нашого
реєстру; код, якого в реєстрі немає, отримує `null` (а не «ranged за
замовчуванням») плюс попередження прогону. Перевірено на LEAVE-001: 8 фактів,
`leave/leave_place/leave_days/unit_to_report -> ranged`,
`leave_actual_return/document_number/document_date/travel_document ->
permanent_event`.

**Регресія блоку:** 251 passed / 1 xfailed; leave 224/224; deployment
183/183; нормативний корпус 41/1.

## Блок «інжест і статуси» (C-04, C-05, A-19)

### C-04 — `confirmed` при `facts: []` [ЗАКРИТО з нашого боку]

Новий ключ `meta["facts_expected"]` розводить три стани явно: `True` + факти
є (звичайний документ), `True` + порожньо (ПОРУШЕННЯ витягу), `False` +
порожньо (НОРМА: нормативний акт). `None` — до питання не дійшли (дублікат,
нечитабельний файл). Перевірено: квиток `confirmed / True / 8 фактів`,
нормативний `confirmed / False / 0`.

Друга половина знахідки — перевірка `чернетка_не_факт`, яка називає порушенням
саме те, що пайплайн навмисно виробляє — живе в `eval/evaluate.py`, тобто в
ділянці іншого виконавця. Ключ для неї тепер є.

### C-05 — сторінка з текстом І растром губила растр мовчки [ЗАКРИТО як видимість]

**Репро ДО:** PDF на одну сторінку з текстовим шаром (69 символів) і растром:
`ocr_fn` не викликано, `ocr_pages: 0`, `source_kind: electronic`, нуль
попереджень. **ПІСЛЯ:** ті самі цифри, але `text_pages_with_images: [1]` і
попередження «растр без OCR на сторінках № 1 … усе, що намальовано на растрі,
у текст не потрапило». Порожні сторінки теж більше не німі
(`empty_pages`).

`scan_pages_detected` СВІДОМО не змінено: замір по всіх 1122 сторінках PDF
репозиторію — 31 сторінка «текст + растр», і всі до одної в нормативних актах
(штамп/логотип на першій сторінці), у факт-документах жодної. Зарахувати їх у
скани означало б перевести born-digital закон у `photo`, тобто в чужу планку
якості й у чергу «рукописне» — регресія, не захист. Чи OCR-ити растр на
текстовій сторінці — у «потребує рішення».

### A-19 — OCR без межі й без прогресу [ЗАКРИТО]

`ocr.max_pages` (дефолт `null` = попередня поведінка) + посторінковий
`print` прогресу. Перевірено на синтетичному 3-сторінковому скані:
`max_pages=2` → `ocr_pages: 2`, `pages_over_ocr_limit: [3]`, попередження
«документ оброблено ЧАСТКОВО»; `null` → 3 сторінки, попереджень немає.
Посторінкове ЗБЕРЕЖЕННЯ не робилось (див. «потребує рішення»).

**Регресія блоку:** 251 passed / 1 xfailed; leave docx 224/224, pdf 224/224;
deployment pdf 183/183.

## Блок «провенанс і чесність інтерфейсів» (C-08, A-02, A-03, A-05, A-17, C-09)

### C-08 [ЗАКРИТО]

- Поріг 20 символів названо константою `MIN_DOCUMENT_TEXT_CHARS` і
  обґрунтовано ЗАМІРОМ: найкоротший текст документа, з якого пайплайн витягує
  факт, — 875 символів (`довідка_лікування_02.docx`), тобто межа стоїть на
  іншому боці зазору в 40 разів, а не «на око».
- Основний факт позначено явно: `facts[*].is_primary`, і `run.py` шукає його
  позначкою, не індексом. Порядок (основний першим) лишився — але тепер він не
  єдиний носій цього знання. Перевірено: LEAVE-001 → `leave: is_primary=True`,
  сім похідних `False`.

### A-02 [ЗАКРИТО, латентне]

`meta["extra_subjects"]` тепер існує й заповнюється з `record`. Дірка досі
латентна (жодна схема не оголошує `group:`, значення завжди `{}`) — саме тому
проведення ключа дешеве: коли групу оголосять, друга особа не зникне мовчки.

### A-03 [ЗАКРИТО в частині обґрунтування]

Стале обґрунтування прибрано з 9 місць у схемах (3 + 6) і з двох коментарів
`build_record.py`: колонка `facts.additional_info JSONB` існує (міграція
`8a667569ba4d`) і завантажувач її пише (`ai_secretary_loader.py:497`). Вибір
між `dimension:` і `additional_info` тепер описаний правдиво: «окремий рядок,
за яким можна ЗАПИТАТИ» проти «поле в JSON, яке видно, але за яким не
запитаєш». Саме ДУБЛЮВАННЯ реквізитів — у «потребує рішення».

### A-05 [ЗАКРИТО звуженням]

**Репро ДО:** `parse_rank_and_name('молодший сержант Гайдученко Остап
Миронович')` → звання є, усі три частини ПІБ `None` (саме так ПІБ надрукований
у holdout-формі). **Фікс:** позиційний розбір із провенансом
`positional_name_no_uppercase`, який доданий до `build_record.UNRELIABLE_METHODS`
(імпортом, не літералом) — значення ВИДНО, але воно не дає `confirmed`, поле
йде в локалізовану прогалину для LLM. Фільтр `positional_name_parts` пускає
лише суто літерні токени з великої, тобто клас помилки, через який позиційний
розбір колись і прибрали (`'25О'`, `'від'`, `'О7.О5.2О2б'`), не повертається.
**ПІСЛЯ (наскрізно, LEAVE-001 з прізвищем із малої):** три частини ПІБ у
записі, `resolved: false`, документ `needs_review`, факт `confirmed: false`.

Оголошуваність правила (`surname_marker:` у схемі) НЕ робив свідомо: ключ із
єдиним підтримуваним значенням — це рівно той мертвий ключ, за який A-04
щойно зробив помилку валідатора.

### A-17 [ЗАКРИТО] — `domain_scores` є і в успішному поверненні `identify_template`.

### C-09 [ЗАКРИТО]

`--dry-run` більше не підміняє сховище на `None` (через що гілка `duplicate`
була недосяжна за побудовою, тобто сухий прогін мовчки означав ще й
`--reprocess`). Тепер сухий прогін вимикає лише ЗАПИС: `res["read_only"]`,
який читає `_persist`. **Репро ПІСЛЯ:** робочий прогін LEAVE-003 →
`confirmed`; сухий прогін того самого файлу → `duplicate`. Сухий прогін
нічого не пише (перевірено по індексу сховища).

**Регресія блоку:** 251 passed / 1 xfailed; leave docx 224/224, pdf 224/224;
deployment docx 183/183, pdf 183/183; нормативний корпус 41/1.
