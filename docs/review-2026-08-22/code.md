# Рев'ю коду, 22.08.2026

Незалежний рецензент. Читано: `pipeline/**`, `eval/**`, `run_pipeline.py`,
`pipeline/schemas/*.yaml`, `pipeline/dictionaries/*.yaml`,
`demos/upload_app/*.py`. Заборонені файли (`docs/known-weak-spots.md`,
`docs/open-questions.md`, `docs/research/**`, `docs/tasks/**`,
`docs/contracts/**`, git-історія) не відкривались.

## Що прогнано

| прогін | результат |
|---|---|
| `python -m pytest eval/tests -q` | `225 passed in 10.11s` |
| `python -m eval.evaluate --input data/eval/samples/leave/synthetic-2026-05/docx --no-llm` | `224/224 (100.0%)`, шаблон 16/16, статуси `{'confirmed': 15, 'needs_review': 1}` |
| `python -m eval.evaluate --input data/eval/samples/deployment/synthetic-2026-05/docx --no-llm` | `183/183 (100.0%)`, шаблон 14/14, статуси `{'confirmed': 13, 'needs_review': 1}` |
| `run_pipeline.py --no-llm --dry-run --reprocess` на `leave/відпускний_шаблон.docx` | `REV ... критичні прогалини: [8 полів]` — правильно |
| те саме на `normative/інструкція_діловодство.docx` | `OK ... procedural_document:normative` (статус **confirmed**) |
| те саме на `demo-story/story/DEMO-01.docx` | `OK ... leave_ticket`, некритичні прогалини |
| те саме на `holdout/довідка_лікування_01.docx` | `UNRS ... below_llm_floor` — правильно |
| те саме на `leave/відпускний_квиток_інша_редакція.docx` | `REV`, coverage 0.148 проти 0.5, значення → `unverified_foreign_edition` — правильно |

Записи в `data/output/` відкриті й звірені очима з текстом документів
(`відпускний_шаблон`, `DEMO-01`, `інструкція_діловодство`,
`довідка_лікування_01`). Провенанс на цих чотирьох збігається з тим, що
фізично стоїть у документі. Прогони з `--ocr surya` і з увімкненою моделлю
не робились (ресурс зайнятий) — знахідки, що їх вимагали б, позначені
`confidence: medium`.

Прилад і тести — чисті. Усе, що нижче, прилад **не карає**: усі чотири
критичні знахідки живуть на входах, яких у еталонному корпусі немає, і на
станах, які прилад не перевіряє.

---

```
id: C-01
severity: critical
where: pipeline/identification.py:641-655 (перевірка `kind == "procedural"` ПЕРЕД вибором шаблону) + pipeline/run.py:665-671 (`status="confirmed" if procedural`)
claim: Справжній факт-документ, у якому трапилось ДВА формальні маркери нормативного акта, оголошується нормативним текстом і виходить зі статусом `confirmed`, з нулем фактів і БЕЗ черги рев'ю.
evidence: |
  identification.py: перевірка процедурного домену стоїть до scoring шаблонів
  і повертає одразу, ігноруючи будь-який анкорний бал:
      if coarse_domain and (domains.get(coarse_domain) or {}).get("kind") == "procedural":
          return {"schema": None, ... "reason": f"procedural_document:{coarse_domain}"}
  classify.py:170-181: процедурний вирок дається за `formal_markers >= 2`
  (`min_formal_markers: 2` у domain_keyphrases.yaml:169), БЕЗ жодного гейту
  довжини й без змагання з тематичними доменами. Серед маркерів —
  'наказую:', 'про внесення змін', 'набирає чинності', 'цей порядок',
  'терміни та визначення'.
  Прогін (реальний LEAVE-001.docx, анкорний бал 15 з 15):
      BASE: leave_ticket None {'deployment_certificate': 0, 'leave_ticket': 15}
      WITH '\nНАКАЗУЮ:\nНабирає чинності з дня опублікування.\n'
           -> None procedural_document:normative normative
      WITH '\nПро внесення змін до Інструкції.\nЦей порядок визначає порядок надання відпусток.\n'
           -> None procedural_document:normative normative
  run.py далі дає такому запису: status="confirmed", review_queue=None,
  review_reason=None, facts=[], subject_kind="none".
impact: |
  На реальному потоці частини це не екзотика: витяг із наказу, наказ
  командира про надання відпустки, будь-який службовий документ, що містить
  «НАКАЗУЮ:» плюс одну посилальну конструкцію, — набирає два маркери. Такий
  документ зникає з підрахунків МОВЧКИ і в найгіршій з можливих форм:
  * `status: confirmed` — лоадер не позначить його як збій;
  * `review_queue: null` — у чергу рев'ю не потрапить, людині його не покажуть;
  * `facts: []` — у підрахунок «скільком людям зараз у відпустці» не додасть
    нічого;
  * 5%-аудит його теж не візьме (`_sampled_for_audit` викликається лише в
    гілці зі схемою, run.py:818).
  Тобто людина не побачить нічого. Ні цифри, ні прогалини, ні черги. Це
  прямо суперечить правилу «відмова краща за вигадку»: система не відмовляє,
  вона тихо каже «тут нема про що говорити». Прилад цього не ловить, бо в
  корпусі `data/eval/synthetic-2026-05` нормативних документів немає взагалі,
  а перевірка `чернетка_не_факт` для такого запису дала б `ok: false`
  (`consistent = bool(fact_flags) and ...` при `facts=[]` — див. C-04).
proposal: |
  1. Процедурний вирок не має бути безумовно старшим за анкорний. Якщо
     шаблон набрав >= min_score І має хоч один анкор — це позитивне
     свідчення про БЛАНК, і воно сильніше за два маркери жанру; тоді
     процедурний домен має або програти, або дати `ambiguous`/`unresolved`,
     але не `confirmed`.
  2. Незалежно від (1): статус `confirmed` для документа з `facts: []`
     небезпечний за побудовою. Потрібен окремий статус (напр.
     `no_facts_expected`) або `review_queue` хоча б для вибіркового аудиту,
     інакше жоден процедурний вирок ніколи не перевіряється людиною.
  3. `formal_markers` варто розділити на «маркери самого акта» ('верховна
     рада україни постановляє', 'зареєстровано в міністерстві юстиції',
     'нд тзі') і «згадувальні» ('наказую:', 'про внесення змін',
     'набирає чинності'), з різною вагою: перші в бланку не трапляються
     ніколи, другі трапляються.
confidence: high
```

```
id: C-02
severity: critical
where: pipeline/schemas/leave_ticket.yaml:314 (`supersession_note`), pipeline/schemas/deployment_certificate.yaml:253 (те саме поле посвідчення); наслідок — pipeline/build_record.py:509-522
claim: Незаякорений однослівний regex на весь текст документа створює зв'язок `supersedes` (скасування ІНШОГО документа) від будь-якої згадки слова «анульовано»/«переоформлено»/«перервана», причому запис лишається `confirmed`.
evidence: |
  Патерн: '(?P<value>перервана|відкликаний з відпустки|анульован\w*|переоформлено)'
  — без межі слова, без прив'язки до лейбла, шукається в ocr_text цілком.
  Прогін на реальному LEAVE-001.docx з дописаним рядком:
      ''                                        | confirmed: True | links: []
      'ВПД анульовано, видано нові.'            | confirmed: True | links:
          [{'link_type': 'supersedes', 'target_document_number': None,
            'source_field': 'supersession_note', 'evidence': 'анульовано',
            'method': 'matched'}]
      'Порядок анульовання квитків визначено інструкцією.' | confirmed: True
          | links: [{'link_type': 'supersedes', ... 'evidence': 'анульовання'}]
  Перший рядок — про ВПД (проїзні документи), не про сам квиток. Другий —
  взагалі процедурна згадка, і збігається лише тому, що `анульован\w*`
  ловить «анульовання».
impact: |
  `supersession_note` — `criticality: optional`, тож хибний зв'язок не
  знижує ні статус документа, ні `facts[0].confirmed`. Запис іде в базу як
  `confirmed` з ознакою «цей документ скасовує попередній квиток цієї
  особи», і сторона БД за цією ознакою закриває ПОПЕРЕДНІЙ факт відпустки
  тієї самої людини (номера немає — `target_document_number: None`, пару
  шукають за особою й датами, як прямо сказано в build_record.py:513-515).
  Людина не побачить: статус `confirmed`, `review_queue: null`, у warnings
  нічого, `document_links` — це «нормальне» непорожнє поле. Наслідок —
  цифра «скільком людям зараз у відпустці» стає МЕНШОЮ за правду, і саме
  тим способом, який неможливо помітити на виході: обидва документи в базі
  виглядають правильно, зіпсований лише зв'язок між ними. Прилад це не
  ловить: перевірка `зв'язок_скасування` міряє лише «є зв'язок / немає
  зв'язку» проти поля `пара.replaces` еталона, а в корпусі немає жодного
  документа зі згадкою цих слів поза справжньою позначкою скасування.
proposal: |
  1. Мінімум — межа слова: `(?<!\w)(?:перервана|...)(?!\w)`, і прибрати
     `\w*` там, де воно ловить іменник замість дієприкметника
     (`анульован(?:о|ий|ого|а)`).
  2. Головне — поле мусить бути ЗАЯКОРЕНЕ. Позначка скасування на бланку
     стоїть у конкретному місці; або `extraction: block_before_label` з
     лейблом бланка, або regex із обов'язковим контекстом
     (`квиток\s+(?:анульовано|переоформлено)`), а не голе слово в 1300
     символах тексту.
  3. Незалежно від (1)-(2): `link_type` — це вказівка ЗАКРИТИ чужий факт.
     Такий зв'язок не має права виходити з `confirmed`-документа без
     жодного сигналу; або поле стає `critical`, або наявність
     `document_links` сама додає документ у чергу рев'ю.
confidence: high
```

```
id: C-03
severity: critical
where: pipeline/schemas/leave_ticket.yaml:241,253 і pipeline/schemas/deployment_certificate.yaml:288,296 (`document_number`, `document_date`)
claim: Номер і дата САМОГО документа беруться першим збігом «№ X від DD.MM.YYYY» у всьому тексті, тому номер ЗГАДАНОГО наказу стає номером цього документа з провенансом `matched`, confidence 0.9 і статусом `confirmed`.
evidence: |
  Патерни: '№\s*(?P<value>[\w/\-]+)\s+від\s+\d{1,2}\.\d{1,2}\.\d{4}' і
  парний до нього для дати. `extract_field_regex` робить `pattern.search(text)`
  — перший збіг у документі, без прив'язки до місця на бланку.
  Прогін на реальному LEAVE-001.docx з дописаним ПЕРЕД текстом рядком
  «Відповідно до наказу командира № 777/К від 01.01.2020 р.»:
      doc_number: ('777/К', 'matched') doc_date: ({'day': '01', 'month': '01', 'year': '2020'}, 'matched')
      confirmed: True
      prov: {'method': 'matched', 'criticality': 'optional', 'resolved': True, 'confidence': 0.9}
  Правильні значення того ж документа — № 102 від 09.05.2026.
impact: |
  Це не «порожнє поле», а тихо ПІДМІНЕНЕ, з найвищою довірою в системі.
  Обидва поля `optional`, тож `confirmed` лишається True; обидва
  оголошують `dimension:`, тож ідуть у базу ОКРЕМИМИ підтвердженими
  фактами (`document_number`, `document_date` — видно в записі
  `data/output/documents/leave/e1d6e74f-....md`). Наявний шар узгодженості
  тут безсилий за побачення: єдине правило, що торкається `document_date`
  — `not_before` (leave_start_date >= document_date), і підставна ДАВНІША
  дата його проходить (2026-05-10 >= 2020-01-01). Наслідок для продукту:
  «посилання на документ-джерело» — один із чотирьох обов'язкових
  елементів складу відповіді (README) — вказує на ЧУЖИЙ документ. Людина
  не побачить, бо статус `confirmed`, черги немає, провенанс каже `matched`
  — тобто найсильніше можливе твердження «прочитано з бланка».
  Клас цієї помилки вже описаний у коментарі до `UNANCHORED_MODES`
  (extract.py: «у чужій редакції такий патерн збігається з ПЕРШИМ, що
  схоже за формою — напр. із номером ЗГАДАНОГО документа замість номера
  цього»), але страховка вмикається ЛИШЕ при `form_recognized=False`.
  На впізнаному бланку — рівно та сама вада без жодної страховки.
proposal: |
  1. Взяти `find_block_before_label`/позицію на бланку замість
     `search()` по всьому тексту, або обмежити пошук ділянкою до першого
     друкованого рядка бланка (`printed_cutters` уже дає порядок рядків).
  2. Дешевша проміжна міра: якщо патерн має БІЛЬШЕ ОДНОГО збігу в
     документі — це `ambiguous`, а не «беремо перший». Той самий висновок
     уже зроблено для лейблів (`ambiguous_label`), і він тут доречний
     дослівно.
  3. Додати правило узгодженості «document_date у межах N днів від
     leave_start_date» — `not_before` односторонній і цей клас не бачить.
confidence: high
```

```
id: C-04
severity: should-fix
where: eval/evaluate.py:654-667 (`чернетка_не_факт`) проти pipeline/run.py:666-671
claim: Прилад оголошує стан «status=confirmed при порожньому facts» порушенням інваріанта, а пайплайн навмисно його виробляє для кожного нормативного документа — тобто на цьому класі документів прилад і код суперечать один одному, і жоден прогін цього не показує.
evidence: |
  evaluate.py:
      fact_flags = [bool(f.get("confirmed")) for f in facts]
      if meta.get("status") == "confirmed":
          consistent = (bool(fact_flags) and all(fact_flags)
                        and not (meta.get("unknown_critical_fields") or []))
  При `facts == []` -> `bool([])` -> False -> перевірка ПРОВАЛЮЄТЬСЯ.
  run.py для процедурного документа:
      status="confirmed" if procedural else "unresolved", ... facts не
      заповнюються взагалі (base_meta["facts"] == []).
  Заміряно: `run_pipeline.py --input .../інструкція_діловодство.docx` ->
  `OK ... procedural_document:normative`, `Підсумок: confirmed=1`,
  у записі `data/output/documents/normative/9d8c...md` — `facts: []`.
impact: |
  Дві різні речі, обидві погані. Перша: якщо в еталонний корпус колись
  додадуть нормативний документ (а це наступний очевидний крок — у
  `data/eval/samples/normative/` уже 43 файли), прилад покаже провал на
  правильній поведінці, і його «виправлять» послабленням саме тієї
  перевірки, яка ловить R-A1-01. Друга, важливіша: зворотний бік цієї
  дірки — гілка `else: consistent = not any(fact_flags)` теж проходить на
  порожньому `facts`, тобто пайплайн, який на ЯКОМУСЬ класі документів
  перестав віддавати факти взагалі, отримає 100% від приладу. Разом із
  C-01 це і є механізм, яким тихе зникнення документа лишається
  незаміряним.
proposal: |
  Розвести три стани явно, і в приладі, і в run.py: `confirmed з фактами`,
  `confirmed без фактів, бо фактів у документі немає за визначенням`
  (окремий статус або окремий ключ `facts_expected: false`), `порожній
  facts там, де факти очікувались` — останнє мусить бути провалом
  перевірки. Поки статус `confirmed` означає обидві перші речі, інваріант
  «чернетка ≠ факт» неможливо перевірити на цьому класі.
confidence: high
```

```
id: C-05
severity: should-fix
where: pipeline/ingestion/ingest.py:251-296
claim: Рішення «текстовий шар АБО скан» приймається по сторінці як «або-або», тому сторінка, у якої є І текстовий шар (>= 40 символів), І растр, втрачає растр цілком — без попередження, без ocr_pages, без `scan_pages_detected`.
evidence: |
      page_text = page.get_text() or ""
      if len(page_text.strip()) >= MIN_TEXT_CHARS_PER_PAGE:   # 40
          ...page.get_text("blocks")...
          continue          # <- page.get_images() тут уже не питається
  Симетрична дірка нижче:
      if not page.get_images():
          continue          # <- ні warning, ні pages_needing_ocr
  Порівняйте з гілкою `pages_needing_ocr`, яка про пропуск сторінок КАЖЕ
  вголос (рядки 312-326). Тут не каже нічого.
impact: |
  Реальний вхід замовника — це саме змішані сторінки: сканований бланк, на
  який сканер додав текстовий колонтитул/штамп, або PDF, зібраний з
  текстової першої сторінки й фотографії зворотного боку, вклеєної як
  растр на сторінку з підписом-текстом. 40 символів колонтитула досить,
  щоб уся сторінка пішла «текстовим» шляхом, і зворотний бік бланка (дата
  повернення, відмітки про постановку на облік) зникає. Далі документ
  виглядає як звичайний `electronic` (бо `scan_pages_detected` не
  виставився), а поля зворотного боку — як звичайні прогалини. Людина
  бачить прогалини, але не бачить ПРИЧИНИ: у warnings нічого, у
  `ocr_pages: 0`, `source_kind: electronic`. Тобто «OCR не робили» і
  «OCR зробили погано» знову злипаються — рівно та проблема, яку
  `ocr_blocks`/`ocr_chars` вирішили для одиночних зображень.
proposal: |
  Розділити два питання: «чи є на сторінці текст» і «чи є на сторінці
  растр». Якщо є обидва — або обробити обидва (текстові блоки + OCR
  растра, з `source`-мітками, які extract.py уже вміє розрізняти), або як
  мінімум додати попередження «сторінка N має зображення, не пройдене
  OCR» і врахувати її в `scan_pages_detected`. Порожню сторінку без
  растра теж рахувати окремим ключем (`empty_pages`), щоб «сторінку
  пропущено» ніколи не було німим.
confidence: medium
```

```
id: C-06
severity: should-fix
where: pipeline/extraction/extract.py, `slot_is_provably_empty` / `empty_slot_patterns` (прохід «СХЕМНИЙ ДОКАЗ ПОРОЖНЕЧІ (П5-Б)») проти оголошеного інваріанта в коментарі до `CONFIRMED_EMPTY_SLOT_METHOD`
claim: Коментар обіцяє, що доказ порожнечі вимагає ЛОКАЛІЗОВАНОГО слота, а `empty_pattern` шукається регексом по ВСЬОМУ тексту документа — тобто скелет порожнечі з однієї сторінки доводить порожнечу поля, значення якого стоїть на іншій.
evidence: |
  Обіцянка (extract.py, докстрінг CONFIRMED_EMPTY_SLOT_METHOD):
      «Межа чесності: сигнали вимагають ЛОКАЛІЗОВАНОГО слота. «Слот не
      знайдено» (no_label / no_value / чужа редакція бланка) лишається в
      LLM за побудовою»
  Реалізація:
      def slot_is_provably_empty(field_def, text: str) -> bool:
          return any(p.search(text) for p in empty_slot_patterns(field_def))
  `text` тут — `ocr_text` цілком. Єдиний гейт — `value is None`, тобто
  «детермінований шлях нічого не дав». Але `value is None` означає і
  «значення немає», і «ми не змогли його прочитати» — а саме в другому
  випадку фолбек і потрібен.
  Заміряно, що зараз не стріляє: прогін усіх 16 leave/docx показав збіг
  скелета лише на LEAVE-011 (справді порожньому):
      LEAVE-011.docx EMPTY-SKELETON MATCHES: ['duration_days',
      'leave_start_date', 'leave_end_date_planned', 'actual_return_date']
impact: |
  Відпускний квиток двосторонній («Продовження додатка 30» — видно в
  розпізнаному тексті `відпускний_шаблон.docx`). Документ, у якому лицьовий
  бік заповнений, а другий примірник полів (чи зворотний бік) лишився
  порожнім, дасть збіг скелета порожнечі. Якщо на лицьовому боці regex
  провалився (гомогліфи, склеєний OCR, інша словоформа) — поле не піде ні
  в LLM, ні в підказку, і в провенансі стане
  `confirmed_empty_slot:empty_pattern`, тобто «ДОВЕДЕНО, що на бланку
  цього немає». Це найгірший вид тихої помилки в цьому пайплайні: не
  прогалина, а хибний ДОКАЗ. Рев'юер, побачивши «слот порожній»,
  підтвердить порожнечу й не піде дивитись у документ. Прилад цього не
  міряє: у корпусі немає документа з одночасно заповненим і порожнім
  слотом того самого поля.
proposal: |
  Прив'язати `empty_pattern` до тієї самої області, у якій шукалось
  значення (блок лейбла / сторінка), а не до всього тексту. Мінімальна
  версія: якщо скелет порожнечі збігається БІЛЬШЕ ОДНОГО разу, або
  збігається на сторінці, відмінній від тієї, де стоїть лейбл поля, —
  доказом він не є, і поле йде у звичайний фолбек. Плюс явна перевірка,
  що при `form_recognized=False` жоден сигнал П5-А/П5-Б не оголошує
  порожнечу (зараз це тримається на емпіриці одного файлу, а не на коді:
  прогін `відпускний_квиток_інша_редакція.docx` дійсно не дає жодного
  `confirmed_empty_slot`, але жодна умова цього не гарантує).
confidence: medium
```

```
id: C-07
severity: should-fix
where: pipeline/extraction/extract.py, `attested_numbers` / `ground_llm_value`
claim: Заземлення числа перевіряє наявність числа В УСЬОМУ ДОКУМЕНТІ, тому вигадане значення, що випадково дорівнює дню, місяцю чи частині номера документа, проходить як витягнуте.
evidence: |
  Прогін на тексті
  «Відпускний квиток № 4180/26 від 31.07.2026. Терміном на дванадцять діб.»
  з полем {"type":"number","min_value":1,"max_value":366}:
      attested: [7, 12, 26, 31, 2026, 4180]
      12   -> (12, None)                       # правильне
      26   -> (26, None)                       # з «4180/26»
      31   -> (31, None)                       # день з дати
      7    -> (7, None)                        # місяць з дати
      4180 -> (None, 'ungrounded_llm_value')   # відкинуто лише межами поля
      2026 -> (None, 'ungrounded_llm_value')   # відкинуто лише межами поля
  Тобто з шести чисел документа заземлення відсіює тільки ті два, що не
  влазять у min_value/max_value. Решта — вільний вибір моделі.
impact: |
  Заміряний випадок, заради якого заземлення й писали (LEAVE-011,
  `днів = 17`), відсіявся лише тому, що «17» у тому документі не було.
  Якби модель віддала «19» (день з «19.05.2026»), заземлення пропустило б.
  Сьогодні клас закритий ДРУГИМ шаром: обидва number-поля наявних схем
  (`duration_days`, `deployment_days`) мають `consistency: days_span_inclusive`,
  тож вигадка без дат отримує `unverifiable_dependency`. Але це збіг
  конфігурації, не властивість заземлення: перше число без правила
  узгодженості (кількість осіб, кількість зупинок, номер прописом) буде
  прийняте з провенансом `llm` і confidence 0.6, і документ може вийти
  `confirmed`. Людина не побачить: провенанс каже `llm`, не
  `ungrounded_llm_value`.
proposal: |
  Заземлювати число в ОКОЛИЦІ, а не в документі: якщо для поля відома
  позиція (лейбл, підказка локалізованої прогалини) — перевіряти
  наявність числа саме там. Для нелокалізованих прогалин — принаймні
  вимагати, щоб число стояло не всередині дати/номера документа (тобто
  щоб токен, у якому воно знайдене, не був частиною збігу
  `_DATE_PATTERNS` чи `№ ...`). І зробити правило узгодженості
  обов'язковим для нових number-полів на рівні валідатора схем —
  зараз воно необов'язкове, тобто другий шар з'являється лише за доброю
  волею автора схеми.
confidence: high
```

```
id: C-08
severity: should-fix
where: pipeline/run.py:594-605 (гейт `len(text.strip()) < 20`) і pipeline/run.py:727
claim: Два пороги, від яких залежить, чи документ узагалі отримає факт, узяті з голови й ніде не обґрунтовані; другий («основний факт — це `facts[0]`») ще й робить порядок полів у YAML частиною контракту з базою.
evidence: |
      if not text or len(text.strip()) < 20:
          ... status="unresolved" ...
  20 — єдине число в цьому файлі без жодного рядка обґрунтування, при тому
  що поруч кожен поріг має заміряне походження (MEGA_BLOCK_HEIGHT_RATIO,
  MIN_PLAUSIBLE_OCR_BLOCKS, DEFAULT_MIN_BLANK_COVERAGE — усі з цифрами).
      confirmed = bool(record["facts"]) and record["facts"][0].get("confirmed")
  плюс build_record.py:605-606 («завантажувач споживача бере facts[0]»).
impact: |
  Перше: документ, з якого OCR дав 15-19 символів (кутовий штамп і більш
  нічого), і документ, з якого OCR дав 21 символ, ідуть різними гілками —
  перший в `unresolved` з причиною «збій розпізнавання», другий в
  ідентифікацію, де майже гарантовано отримає `below_llm_floor`, тобто
  ІНШУ причину в черзі. Різниця в причині вирішується випадковим числом.
  Друге, важливіше: `facts[0]` як «основний факт» тримається виключно на
  тому, що `build_record` повертає `[fact] + extra_facts`. Жоден валідатор
  схем цього не перевіряє. Схема, у якої `db_target: fact_value` стоїть
  після поля з `dimension:`, порядок не змінить (extra_facts збираються в
  циклі, основний додається попереду), але будь-яка зміна в цьому місці
  build_record тихо переставить, ЧИЙ `confirmed` визначає статус
  документа, і ЧИЙ факт лоадер візьме як джерело дати. Мовчазний зв'язок
  через індекс списку між трьома модулями і чужим лоадером.
proposal: |
  1. Або обґрунтувати 20 заміром, або замінити на осмислену умову
     (напр. «менше ніж MIN_PLAUSIBLE_OCR_CHARS на сторінку» — таке число
     вже є й заміряне), або прибрати гейт і дати ідентифікації сказати
     `below_llm_floor` самій.
  2. Позначити основний факт ЯВНО (`fact["role"] = "primary"` або окремий
     ключ `record["primary_fact"]`), а не позицією в списку, і додати це
     в контракт із лоадером.
confidence: high
```

```
id: C-09
severity: nit
where: pipeline/run.py:548 (`existing_key = store.find_by_hash(file_hash) if store else None`) у поєднанні з run_pipeline.py:78 (`--dry-run` -> `res["store"] = None`)
claim: `--dry-run` мовчки вимикає не лише запис, а й дедуплікацію — той самий файл у `--dry-run` ніколи не буде «duplicate», хоча в звичайному прогоні буде.
evidence: |
  run_pipeline.py:
      if args.dry_run:
          res["store"] = None
  run.py:
      existing_key = store.find_by_hash(file_hash) if store else None
      if existing_key and not reprocess: ... status="duplicate"
  Тобто `--dry-run` = `--reprocess` за побічним ефектом.
impact: |
  `--dry-run` заявлений як «нічого не зберігати», а насправді ще й міняє
  ВЕРДИКТ пайплайна: прогін, яким людина перевіряє «що вийде з цієї
  папки», ніколи не покаже `DUP`, тобто не покаже саме той клас проблеми
  (той самий документ уже оброблений), заради якого сухий прогін і
  роблять. Той самий побічний ефект має прилад (`res["store"] = None`,
  evaluate.py:766) — там він навмисний і задокументований, тут — ні.
proposal: |
  Розділити «сховище для читання» і «сховище для запису»: `--dry-run`
  лишає `find_by_hash`, але не викликає `save`/`retire`. Або хоча б
  сказати вголос: «--dry-run: дедуплікація вимкнена».
confidence: high
```

```
id: C-10
severity: nit
where: pipeline/run.py:772-786 (`create_subject_object`) на порожньому бланку
claim: Порожній бланк без жодної літери ПІБ виходить із `create_subject_object: true`.
evidence: |
  `data/output/documents/leave/f73220f5-....md` (прогін
  `відпускний_шаблон.docx`):
      subject_kind: person
      subject_kind_source: schema
      create_subject_object: true
      subject: {rank: null, surname: null, given_name: null,
                patronymic: null, person_alias: null, person_complete: false}
impact: |
  Логічно це послідовно (`subject_kind` — оголошення схеми, і воно про
  ВИД, не про наявність), і в коментарі `creates_object` це прямо
  сказано: лоадер мусить читати обидва ключі. Але ключ називається
  «створювати об'єкт», а об'єкта в документі немає жодного, і надійність
  тут спирається на дисциплінованість чужого коду. Запис при цьому
  `needs_review`, тому реального шляху в базу немає — звідси nit.
proposal: |
  Або перейменувати ключ так, щоб він не читався як команда
  (`subject_object_expected`), або дописати в нього кон'юнкцію з
  «у subject є хоч одне непорожнє поле» для виду `person`. Другого варта
  окремої розмови з командою БД — це зміна контракту.
confidence: high
```

```
id: C-11
severity: nit
where: eval/evaluate.py:966 (`return 0`) і рядки 778-780
claim: Прилад завжди виходить із кодом 0, крім випадку «не знайдено файлів», тому ні провал точності, ні проблеми мапінгу не можуть зупинити нічого автоматичного.
evidence: |
      map_problems = check_mapping(mapping, res["schemas"], truth)
      for p in map_problems:
          print(f"[МАПІНГ] {p}", file=sys.stderr)
      ...
      return 0
  Перевірено побічно: прогін на `data/eval/samples/deployment/synthetic-2026-05`
  (папка з підпапками, тобто нуль документів) дав `exit 2` — а прогін
  із результатом дає 0 незалежно від цифр.
impact: |
  `check_mapping` існує саме тому, що «опечатка в мапінгу дає тихий 0%», і
  сам його результат теж тихий: у пакетному прогоні (CI, хук, скрипт)
  повідомлення `[МАПІНГ]` губиться в stderr, а код виходу каже «все
  добре». Те саме для «ПОЛЯ НА НУЛІ» — блок надрукований, але не
  впливає ні на що.
proposal: |
  Прапорець `--fail-under N` (частка правильних полів) і ненульовий код
  виходу при непорожньому `map_problems`. Достатньо, щоб прилад можна було
  повісити на pre-push.
confidence: high
```

## Що прилад НЕ міряє (окремо, бо це не окремий баг)

1. **Нормативні документи** — жодного в корпусі, а саме на них живе C-01
   і C-04. `data/eval/samples/normative/` має 43 файли, еталонних відповідей
   до них немає.
2. **Документи, які пайплайн віддає без фактів** — перевірка
   `чернетка_не_факт` на порожньому `facts` проходить у гілці «не
   confirmed» (`not any([])` == True) і провалюється в гілці «confirmed».
   Обидві поведінки незаміряні, бо таких документів у корпусі немає.
3. **`document_links` як ЗМІСТ** — міряється лише «є / немає» проти
   `пара.replaces`. Хибний зв'язок на документі БЕЗ пари ловиться, але
   хибний зв'язок на документі З парою (не той target) — ні. C-02 живе
   саме тут.
4. **Похідні факти (`dimension:`)** — `values_by_field` читає значення з
   `facts[0]["additional_info"]`, тобто з ОСНОВНОГО факту. Самі рядки
   `extra_facts` (їхній `value_code`, `fact_type`, `confirmed`,
   `confidence`) не звіряються ні з чим. Саме вони їдуть у базу
   окремими фактами.
5. **Два бланки в одному файлі** — перевірено вручну: конкатенація
   LEAVE-001 + LEAVE-002 дає `leave_ticket`, coverage 0.926, `recognized:
   true`, більшість полів → `ambiguous_label`, `confirmed: False`,
   `unknown_critical: [rank, surname, given_name, patronymic,
   leave_type_and_destination, destination_place]`. Поведінка правильна
   (запис іде в рев'ю), але дати основного факту заповнились ПЕРШИМ
   документом (`date_start: 2026-05-10`, `date_end: 2026-05-22`) без
   жодної ознаки, що у файлі два бланки. У корпусі такого файлу немає.
6. **Той самий файл двічі** — дедуплікація не міряється взагалі: прилад
   навмисно ставить `store = None`.
7. **Межові ПІБ** — перевірено вручну, працює як задокументовано:
   чотиритокенне «рядовий ЛЕМЕШКО Соломія Мустафа кизи» дає
   `_leftover_after_patronymic: ['кизи']` (поле не вирішується);
   «сержант БЕВЗЕНКА Ігоря Петровича» розбирається, відмінок далі
   лікує морфологія; «ЛЕМЕШКО Соломія Романівна» без звання дає
   `rank: None` і правильні три частини. Календарно неможливі дати
   (`31 лютого`, `30.02`) дають `None`. Це все під тестами.
