# Б4 — приймання рев'ю 14.08.2026

Валідатор: фінальна стадія плану `docs/review-plan-2026-08-14.md` (розд. 7).
Гілка `anya-pipeline`, HEAD `0a5a645`. Точка «до рев'ю» для всіх діффів —
`ad8d4e3` («Підчищено папку й закріплено план рев'ю перед запуском агентів»,
14.08 21:57).

Нічого не редаговано й не закомічено; цей файл — єдина зміна в дереві.

## Таблиця критеріїв

| # | критерій | результат |
|---|---|---|
| 1 | тести зелені, ≥197 | **PASS** |
| 2 | чотири корпуси `--no-llm`, чисельник = знаменник, цифри як у b3 | **PASS** |
| 3 | жодна знахідка не зникла між стадіями (33 → 27 → outcome) | **PASS** |
| 4 | еталони недоторкані, `git diff` порожній | **PASS** |
| 5 | блок заборон `.gitignore` не ослаблено | **PASS** |
| 6 | вибіркове відтворення (2 critical + 2 should-fix) | **PASS** |
| 7 | інвентар хардкоду не зріс | **PASS** |
| 8 | hold-out: таблиця по 4 файлах + вердикт за 4 пунктами, поведінка досі чесна | **PASS** (з застереженням, див. нижче) |
| 9 | жодної нової мережевої залежності | **PASS** |
| 10 | метрика сліпих (22/27) зафіксована в docs | **PASS** |
| 11 | заморожений тест-сет не використовувався для налагодження | **PASS** (з нотаткою) |
| — | skipped-знахідка R-A1-12 справді вимагає рішення замовниці | **PASS** |

**Вердикт: ПРИЙМАЄТЬСЯ.** FAIL немає. Один незакритий пункт звітності
(hold-out набір `equipment/`) і три спостереження — нижче, вони не блокери.

---

## 1. Тести — PASS

```
python -m pytest eval/tests -q
197 passed in 3.84s
```

Заявлено 197, отримано 197 (база до рев'ю — 152). Нових тестів у
`eval/tests/test_review_fixes.py` — 45 (файл цілком новий, 725 рядків).

## 2. Чотири корпуси — PASS

```
python -m eval.evaluate --input data/eval/samples/<leave|deployment>/synthetic-2026-05/<docx|pdf> --no-llm
```

| прогін | заявлено в b3 | заміряно мною |
|---|---|---|
| leave docx | 224/224, шаблон 16/16 | **224/224 (100%)**, 16/16, статуси `{confirmed:15, needs_review:1}` |
| leave pdf | 224/224, 16/16 | **224/224 (100%)**, 16/16, ті самі статуси |
| deployment docx | 183/183, 14/14 | **183/183 (100%)**, 14/14, `{confirmed:13, needs_review:1}` |
| deployment pdf | 183/183, 14/14 | **183/183 (100%)**, 14/14, ті самі статуси |

Цифри збігаються з b3-execution.md до одиниці. Чисельник = знаменник на всіх
чотирьох.

**Окремо перевірено, що знаменник ВИРІС, а не що перевірки послабили.**
Арифметика зростання сходиться з заявленою причиною: leave 176 → 224 = +48 =
3 нові перевірки × 16 документів; deployment 155 → 183 = +28 = 2 × 14 (для
deployment немає «впд»). Підтверджено по JSON-звіту: 224 перевірки на 16
записів = 14 на документ, серед них присутні по 16 разів `чернетка_не_факт`,
`зв'язок_скасування`, `впд`. `mapping_problems: []`, `surplus: {}`.

Прогін базової лінії на `ad8d4e3` окремим worktree зробити не вдалося
(`git worktree add` падає з `Could not reset index file`, ймовірно через
кириличні шляхи набору) — тому «до» перевірено арифметикою й складом
перевірок, а не повторним прогоном старого коду. Це єдине місце, де я
покладаюсь на непрямий доказ.

## 3. Знахідки між стадіями — PASS

Порахував сам, `grep -oE "R-(A1|A2|B1)-[0-9]+" | sort -u`:

- вхід: a1 — 15 id (`R-A1-01…15`), a2 — 13 (`R-A2-01…13`), b1 — 5
  (`R-B1-01…05`) = **33**;
- у `b2-verdicts.md` присутні **всі 33** id, жодного зайвого;
- записів «по кожній знахідці» — 27 (6 злиттів: A1-01+A2-01, A1-06+A2-05,
  A1-10+A2-12, A1-03+A2-11, A1-05+A2-09, A1-15+A2-13). 15+13+5−6 = 27 ✓;
- рядків `verdict:` — 27, усі `confirmed`; рядків `outcome:` — 27. Тобто
  кожна confirmed має outcome, порожніх немає;
- таблиця b3 має ті самі 27 рядків і 27 різних комітів (fixed 25, skipped 1,
  not-needed 1) — перелічені хеші існують і всі в діапазоні `ad8d4e3..HEAD`.

Понад це: A1 у розділі «Що б я видалив» мав 5 кандидатів, з яких один
(`DECLARED_BUT_UNREAD_KEYS`) не має номера знахідки — його теж закрито
(коміт `03b271b`). Тобто зникло не тільки нічого пронумерованого, а й
непронумероване.

## 4. Еталони й дерево — PASS

```
git log --pretty="%h %ad %s" --date=iso -- data/eval/synthetic-2026-05/
  b4aef77 2026-08-14 11:02:28   (git merge-base --is-ancestor b4aef77 ad8d4e3 -> так)
  6add9c2 2026-08-14 10:35:25
  370c817 2026-08-12
git log -- "data/eval/samples/holdout/*.expected.json"
  0a5a645  — ЄДИНИЙ коміт: створення (27 рядків × 3 файли), змін після немає
git status --short / git diff --stat  -> порожньо
```

Останній дотик до `data/eval/synthetic-2026-05/` — 11:02, тобто **до** старту
рев'ю (21:57); обидва коміти — предки `ad8d4e3`. `*.expected.json` hold-out'а
створені один раз координатором разом зі звітами A1/A2/B1 і після створення не
змінювались — це генерація, не підгонка під результат. Дерево чисте.

## 5. `.gitignore` — PASS

```
git log ad8d4e3..HEAD -- .gitignore        -> порожньо
git log --since=2026-08-14 -- .gitignore   -> 3 коміти, усі ПЕРЕД ad8d4e3
```

Блок заборон на місці й у силі: `*.pdf`/`*.png`/`*.docx`/`*.xlsx`, рівно один
виняток `!data/eval/**`, архіви `*.zip`/`*.rar`/`*.7z`, секрети `.env`,
`.env.*`, `*.key`. Другого винятку для документів не з'явилось.

## 6. Вибіркове відтворення — PASS

Обидві critical (за фінальною оцінкою верифікатора) плюс дві should-fix
мого вибору.

**R-A1-01 + R-A2-01 (critical) — фінальний confirmed не їхав у facts.**
До-стан невідтворюваний: `pipeline/run.py` після всіх гейтів пише
`fact["confirmed"] = False` у КОЖЕН факт, коли локальний `confirmed` погашено
(diff `ad8d4e3..HEAD -- pipeline/run.py`). Тест на тишу —
`test_needs_review_gate_writes_back_into_facts`: запускає справжній
`process_file` на LEAVE-001.docx з monkeypatch `identify_template →
source="llm"` і вимагає `all(f["confirmed"] is False)`. Тест ловить саме тишу:
без правки статус так само був би `needs_review` (щасливий шлях сходився), а
падає рівно на facts. Перевірено запуском — проходить.

**R-B1-02 (critical) — 4-й токен ПІБ «кизи».**
Прямий виклик на робочому коді:
```
parse_rank_and_name('рядовий ЛЕМЕШКО Соломія Мустафа кизи') ->
  ... '_leftover_after_patronymic': ['кизи']
parse_rank_and_name('рядовий ЛЕМЕШКО Соломія Мустафа')      ->
  ... '_leftover_after_patronymic': None
```
Тобто побайтова однаковість двох входів (сам механізм знахідки) зникла.
Тести: `test_name_tail_token_is_not_silently_dropped` (вимагає
`with_tail != without_tail`),
`test_name_tail_blocks_confirmed_and_keeps_raw_text` (хвіст → критична
прогалина + `unresolved_values` + `provenance.raw_text`, `facts[0].confirmed
is False`), `test_three_token_names_unchanged` (основний шлях цілий). Перший
із них — це тест на тишу в чистому вигляді: він порівнює виходи, а не
перевіряє правильне значення.

**R-B1-03 (should-fix) — часткова сума числівника.**
```
number_from_words('двадцять одін') -> None     (було 20 з matched/0.9)
number_from_words('двадцять одна') -> 21
number_from_words('тринадцять')    -> 13
```
До-стан невідтворюваний, основний шлях не зачеплено. Тест
`test_partial_number_word_sum_is_refused` перевіряє саме відмову
(`is None` на трьох дефектних входах), парний
`test_full_number_words_still_parse` — що фікс не з'їв робочі значення.

**R-B1-04 (should-fix) — неможлива дата.**
`normalize_date('31','лютого','2026') -> None` (як і було — фікс не про це),
а слід тепер зберігається: `test_impossible_date_keeps_raw_match_visible`
збирає запис із regex-групами `{'day':'31','month':'лютого','year':'2026'}` і
вимагає `unresolved_values['leave_start_date'] == '31 лютого 2026'` і того ж
у `provenance.raw_text`. Раніше механізм вимагав `isinstance(raw_value, str)`
і для дат не спрацьовував — тест падає рівно на цьому, при повністю
правильному «щасливому» статусі `needs_review`. Проходить.

Додатково: дисципліна комітів справжня — кожен із перевірених комітів
(`a8f3e66`, `30063ec`, `68fd98a`, `ea38651`) містить рівно правку коду +
тести + outcome у b2, без чужих файлів. `ec72d12` (skipped/not-needed) не
чіпає код узагалі — лише 12 рядків у b2.

## 7. Інвентар хардкоду — PASS

Порахував по `git diff ad8d4e3 HEAD -- pipeline/` і по AST обох ревізій:

- **назви шаблонів у коді: 5 → 5**, усі як були в комментарях; у логіці нуль.
  Тобто найгірший вид хардкоду не з'явився;
- **у бік зменшення:** межа `366` знята з генеричного `type: number` і
  оголошена полем схеми (`min_value`/`max_value` у `duration_days` /
  `deployment_days`); правило «за NNNN рік» знято з генеричного
  `match_dictionary`; літерал «термін не розпізнано» зведено в одну
  константу `UNRESOLVED_TERM_MARKER` (було два незалежні входження);
  `normalization: iso_date` (8 рядків) видалено зі схем;
- **сирий лічильник кириличних літералів у `pipeline/**.py` виріс** (≈149 →
  ≈174, майже все — `identification.py`). Переглянув кожен доданий: це
  повідомлення нового валідатора схем (`extraction_limits`, `blank_template`,
  `min_value/max_value`, `normalization`, `not_issued_sentinel`), тобто текст
  для людини про КЛЮЧІ схеми. Ні фрази з бланка, ні назви наших полів, ні
  порядку блоків нашої верстки серед них немає;
- **нових порогів під верстку в `.py` немає.** Єдиний новий поріг —
  `DEFAULT_DOMAIN_MIN_SCORE = 2` у `classify.py`: генеричний, названий,
  перевизначається ключем `min_score` у довіднику;
- **єдине нове знання про конкретну форму — у СХЕМІ, і це дозволений виняток
  (розд. 6b):** новий regex-варіант поля ВПД у `leave_ticket.yaml` знає
  сусідні друковані рядки бланка («Разом з», «Дійсний у разі») і межу
  `{1,60}`. Замір, який це виправдовує, у комментарії поруч: без нього «не»
  їхало в БД значенням ВПД.

## 8. Hold-out — PASS, із застереженням

`b1-stress.md` розд. 1 містить таблицю по всіх 4 файлах довідки (`шаблон` + 3
заповнені) і вердикт за чотирма пунктами розд. 6a: п.1 ТАК, п.2 не застосовне
(бал 0, часткового впізнання не сталося), п.3 ТАК (0 з 0), п.4 ТАК.

Повторний прогін одного файла зараз:
```
python run_pipeline.py --input "data/eval/samples/holdout/довідка_лікування_01.docx" --dry-run --reprocess --no-llm
UNRS довідка_лікування_01.docx | below_llm_floor
```
Дамп запису (той самий `process_file`, `--no-llm`):
```
status=unresolved  reason=below_llm_floor  review_queue=unknown_type
facts=[]  field_provenance={}  unresolved_values={}  warnings=[]
domain=None   (було leave — R-B1-01 закрито)
identification.domain_scores={'leave':1,'deployment':0,'equipment':0,'staffing':0}
subject_kind=unknown  create_subject_object=False
```
Поведінка досі чесна: відмова з причиною, **нуль значень із провенансом
`matched`**, і та єдина неправда, яку B1 назвав (`domain: leave`), тепер
зникла — бал 1 нижче порога 2, домен `None`, а сам бал друкується в записі.

**Застереження (не блокер).** План (розд. 6a, крит. 8) просив вердикт на
ВСІХ трьох наборах без схеми плюс на згенерованій формі. Згенерована форма —
є (4 файли). `normative/` — заміряно частково: A2 прогнав два нормативні
файли, B1 — 40-сторінковий нормативний текст, усі `unresolved` з
`procedural_document:normative`. **`data/eval/samples/equipment/` (4 фото
книги обліку) не заміряно й не оголошено ніде.** План прямо каже, що «не
змогли заміряти» приймається, а «не міряли» — ні. Мій власний прогін без OCR
перевіряє лише гілку «зображення без OCR» (усі 4 → `unresolved` з причиною
«потрібен ocr.engine: surya»), тобто заміром універсальності не є. Закриття
дешеве: або один прогін з `--ocr surya`, або рядок «не змогли заміряти + чому»
в `b1-stress.md`. Оскільки набір за визначенням плану «слабкий hold-out»
(витягати з нього нічого й не мали), приймання цим не блокую.

## 9. Мережеві залежності — PASS

```
git log ad8d4e3..HEAD -- requirements.txt requirements-dev.txt requirements-optional.txt  -> порожньо
git diff ad8d4e3 HEAD -- pipeline/ | grep '^+' | grep -E 'import|requests|urllib|httpx|socket|aiohttp'
  -> лише два внутрішні: from pipeline.extraction.blank_form import …, from pipeline.extraction.extract import …
git diff ad8d4e3 HEAD --stat -- pipeline/ocr/   -> порожньо
```
Те саме по `eval/` (нові імпорти — `os`, `sys`, `eval.evaluate`).
`pipeline/ocr/surya_reader.py` не змінювався взагалі, тобто перевірка
loopback для `SURYA_INFERENCE_URL` (розд. 10 п.2) на місці. Чужі ділянки
(`db/`, `answer/`) у діффі відсутні.

## 10. Метрика сліпих — PASS

Зафіксована в `b2-verdicts.md`, розд. «(2) Метрика сліпих рецензентів»:
27 унікальних, 5 мають відповідник у `docs/known-weak-spots.md` (перелічені з
номерами розділів), **НОВИХ 22 з 27 = 81%**. Там же названі два суміжні
випадки (R-A1-04 проти 2.9, R-B1-03 проти 2.17), які автор свідомо НЕ
зарахував як «вже відомо», з обґрунтуванням — тобто метрика вважена в
консервативний бік, а не роздута.

## 11. Заморожений тест-сет — PASS, з нотаткою

```
grep -rn "LEAVE-005|LEAVE-013|LEAVE-016|TRIP-003|TRIP-009|TRIP-014" docs/review-2026-08-14/
git log ad8d4e3..HEAD --format="%h %s%n%b" | grep <ті самі id>   -> порожньо
git diff ad8d4e3 HEAD -- pipeline/ eval/ | grep '^+' | grep <ті самі id>  -> порожньо
```

У комітах — жодної згадки. У коді й тестах — жодної (нові тести стоять на
LEAVE-001 і на синтетичних записах у tmp). Тобто виконавець (Б3) на них не
дивився, коли підбирав правки.

Нотатка: два згадування є на СТАДІЇ ДІАГНОСТИКИ — A2 називає LEAVE-014/016 і
TRIP-014 як доказ, що `document_links` спрацьовують, а Б2 при верифікації
R-A2-04 друкує `document_links` для LEAVE-013/016. Це не налагодження правки,
а корпусний прогін для доказу знахідки, і план цього не забороняє. Але
формулювання b3 «під час підбирання правок не використовувався» точніше, ніж
«ніхто не дивився»: дивились, на стадії обвинувачення.

## Skipped-знахідка R-A1-12 — рішення справді за замовницею

Перевірив по коду, а не по звіту:
`pipeline/config.py:49` — `"self_consistency_n": 1`; `config.yaml:28` і
`config.example.yaml:31` — той самий ключ із комментарем «>1 = кілька семплів
+ majority_vote (дорожче)». Тобто механізм досяжний **задокументованою
опцією конфіга**, і «мертвим кодом» у сенсі мандату Б3- він не є: видалення
означало б зняти оголошену капабіліті разом із двома станами моделі довіри
(`llm_split_vote` у `UNRELIABLE_METHODS` і `CONFIDENCE_BY_METHOD`). Це
продуктове рішення, а не дефект; Б3 не мав права ухвалити його сам. Питання
сформульоване одним рядком і в b2, і в b3. **Не лінь — правильний скіп.**

Єдина слабкість: верифікатор чесно позначив, що хронометраж «1,5–4 хв на
виклик» він не переміряв, тобто економічна частина аргументу «ніколи не
ввімкнемо» досі не заміряна. Якщо відповідь буде «лишаємо як капабіліті» —
цей замір знадобиться.

---

## Спостереження, які не є критеріями

1. **Б2 не спростував нічого: 33 з 33 confirmed.** Адверсарний за мандатом
   верифікатор із нульовим відсотком відхилень — сам по собі сигнал, що
   поставу «спростувати» витримано слабше, ніж заявлено. На захист: кожен
   запис має власне відтворення (`how_verified` з прямими викликами й
   репро-скриптами), одна суб-теза (A2-01 про `foreign_edition`) чесно
   позначена `unproven`, а дві severity знижено (R-A1-07 → nit, і сам він
   став `not-needed`). Тобто перевірка була, просто сліпі агенти цього разу
   майже не помилялись.
2. **Пункт 2 критерію «достойно» (часткове впізнання → `needs_review` з
   переліком прогалин) не заміряний ніде** — на hold-out'і бал вийшов 0, і
   гілка не спрацювала. Це найцікавіша гілка для «чужої форми», і вона
   лишається неперевіреною на документі, під який не підганяли.
3. **Прогонів з моделлю у Б3 три** (LEAVE-001/011, репро «кизи», репро
   «двадцять одін») — рівно стільки, скільки потрібно було для гілок, що
   `--no-llm` маскує. Я їх не переміряв (мій ліміт — теж три, і я витратив
   нуль): цифри взяті на віру, це єдине місце звіту Б3, яке я не перевіряв
   самостійно.

## Вердикт

**ПРИЙМАЄТЬСЯ.**

Усі одинадцять критеріїв мого мандату — PASS, FAIL немає. Робота Б3
відтворюється: цифри в b3-execution.md збігаються з прогонами до одиниці,
чотири вибрані фікси справді змінюють поведінку (а не лише додають зелений
тест), еталони й `.gitignore` недоторкані, дерево чисте, інвентар хардкоду
зменшився, мережевих залежностей не додано.

На доопрацювання **не повертаю**. Одне зобов'язання звітності, яке треба
закрити окремо й дешево:

- **`data/eval/samples/equipment/` — заміряти або оголосити «не змогли
  заміряти + чому»** в `b1-stress.md` (план розд. 6a/крит. 8 забороняє
  варіант «не міряли»).

Плюс два відкриті питання, які вже сформульовані й чекають не агента:
рішення по `self_consistency_n` (R-A1-12) і правило розкладання
`ORDER_BASIS` (`basis_order_date`/`basis_order_number`, запит A4).
