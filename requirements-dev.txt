# Dev-залежності: те, без чого не запускаються регресійні тести.
# Ставиться ПОВЕРХ requirements.txt, а не замість:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Свідомо мінімальний список. Кожен рядок нижче -- це те, що падає без нього;
# нічого «на майбутнє» тут немає. Причина: 14.08.2026 виявилось, що pytest
# не встановлений і тести не запускались ВЗАГАЛІ, тобто будь-яка правка йшла
# без сітки безпеки. Файл існує саме щоб це не повторилось на іншій машині.

# Власне запуск тестів: `python -m pytest eval/tests -q`.
# Верхня межа -- бо pytest ламає збір тестів між мажорними версіями
# (rootdir/importmode змінювались саме так).
pytest>=8.0,<9

# ПРИМІТКА про те, чого тут НЕМА і чому:
#
# - pytest-cov, pytest-mock, hypothesis, ruff, mypy -- не потрібні. Тести
#   обходяться stdlib (unittest.mock), покриття ніхто не міряє, лінтера в
#   проєкті немає. Зайва dev-залежність -- це ще один спосіб зробити так,
#   щоб тести не запустились на чужій машині.
# - surya-ocr і llama-cpp-python -- НЕ dev-залежності. Тести на самозцілення
#   OCR (eval/tests/test_ocr_selfheal.py) перевіряють чисту функцію
#   `restart_needed` і підставний менеджер, тому працюють без surya й без
#   ваг моделі. Це навмисно: перевірка умови перезапуску не має коштувати
#   1.5 ГБ ваг і хвилин прогону.
# - pyyaml і python-docx тестам ПОТРІБНІ (test_evaluator читає
#   field-mapping.yaml, test_regressions -- схеми пайплайна), але вони вже
#   в requirements.txt як бойові залежності. Дублювати не треба.
