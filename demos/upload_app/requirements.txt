# Залежності ЛИШЕ демо-апки (demos/upload_app). Кореневий requirements.txt
# не чіпаємо. Пайплайн і лоадер мають власні залежності -- див. README апки.
fastapi
uvicorn
python-multipart
# Чат (сторінка /chat): Gradio-вікно (chat_gradio/) + read-only запити до
# Postgres + каталог шаблонів.
# llama-cpp-python НЕ дублюємо: він уже в кореневому requirements.txt пайплайна.
psycopg[binary]
PyYAML
gradio
# Векторний ярус роутера (етап 3 плану): маршрути з examples каталогу,
# encoder локальний (HuggingFaceEncoder; ПАСТКА: дефолтний encoder
# бібліотеки -- платний OpenAI, у нас він перевизначений у
# chat_gradio/vector_route.py). torch/transformers уже стоять у оточенні
# пайплайна -- extra [local] нових важких залежностей не тягне.
# Версія ЗАКРІПЛЕНА: заміри encoder-ів і поріг 0.92
# (docs/research/2026-08-25_encoder-measurement.md) зняті саме на 0.1.2, а
# правило агрегації балів у цієї бібліотеки міняли між версіями. Нова версія
# перед демо = невідомий роутер, тобто наші цифри перестають бути про той
# код, що працює.
semantic-router[local]==0.1.2
