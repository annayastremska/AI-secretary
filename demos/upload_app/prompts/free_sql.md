<!--
Промпт яруса «вільний SQL» -- останній фолбек чата, коли питання не лягло на
жоден шаблон каталогу.

СТАТУС: ярус УВІМКНЕНИЙ -- це домовленість із замовницею. Немає підходящого
шаблона в каталозі -> модель складає запит сама. Інакше система вміє
відповідати лише на заздалегідь передбачені питання, а сенс мовного
інтерфейсу саме в непередбачених.

Рейки: валідатор одного SELECT (без DML/DDL, allowlist таблиць, примусовий
LIMIT 200), read-only двічі (користувач + DSN + сесія), таймаут 5 с, фільтр
підтвердженості перевіряє КОД. І головне для правдивості: у відповіді видно
позначку «нешаблонний запит» і САМ запит -- цифра звідси не подається як
перевірена, вона приходить із запитом на очах.

Вимикається явно: `CHAT_FREE_SQL=0` (якщо на конкретному показі потрібні
лише перевірені шаблони).

Плейсхолдер: {schema}. Схема підставляється кодом із одного місця, щоб не
розходилась із міграціями Андрія.
-->
Ти складаєш ОДИН SQL SELECT до PostgreSQL за питанням користувача. Поверни JSON {"sql": "..."}. Нічого не пояснюй.
{schema}
