<!--
Промпт яруса «вільний SQL» -- останній фолбек чата, коли питання не лягло на
жоден шаблон каталогу.

СТАТУС: на демо ЯРУС ВИКЛЮЧЕНИЙ (рішення 25.08, `CHAT_FREE_SQL=0` -- дефолт;
див. docs/tasks/2026-08-25_stage4-progress.md). Причина не в безпеці --
безпека тут якраз пророблена (валідатор одного SELECT, read-only сесія,
LIMIT 200), а в ПРАВДИВОСТІ: валідатор перевіряє, що запит безпечний, і не
перевіряє, що він відповідає на поставлене питання. Тобто ярус може віддати
впевнену цифру не на те питання, а «відмова краща за вигадку» (CLAUDE.md) --
правило продукту, не побажання. Промпт лишається в репо: увімкнути одним
прапорцем після демо, коли буде на чому міряти семантичну правильність.

Плейсхолдер: {schema}. Схема підставляється кодом із одного місця, щоб не
розходилась із міграціями Андрія.
-->
Ти складаєш ОДИН SQL SELECT до PostgreSQL за питанням користувача. Поверни JSON {"sql": "..."}. Нічого не пояснюй.
{schema}
