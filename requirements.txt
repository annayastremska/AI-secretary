# Мінімум, щоб пайплайн працював: обробка .docx + вся логіка визначення
# шаблону, екстракції, нормалізації, збірки запису й запису в локальне
# сховище. Перевірено: з ЛИШЕ цими двома пакетами `python run_pipeline.py`
# проходить повністю (LLM/OCR-поля лишаються чесно позначеними прогалинами).
pyyaml>=6.0
python-docx>=1.1.0

# --- Опційне, ставити лише за потреби (див. requirements-optional.txt) ---
# llama-cpp-python   -- LLM-шар (+ окремо ваги .gguf, у репо їх немає)
# surya-ocr          -- фото/скани; тягне torch і Pillow, вантажить ~1.5 ГБ ваг
# minio              -- storage.backend: minio замість локальних файлів
# psycopg[binary]    -- database.backend: postgres
